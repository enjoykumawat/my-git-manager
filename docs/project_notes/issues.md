# Work Log

---

### 2026-08-02 - DEV.to: drafted replies for 3 new comments

- **Status**: Completed
- **Description**: `reply_comments.py pending` returned 3 comments across 3 articles, all from the two most recent published pieces. Two from `mads_hansen_27b33ebfee4c9` on the tag-truncation twin-drift post and the `_gh`/`_dev` error-handling post — checked both replies against the actual current `server.py` before answering rather than assuming the fixes already covered what was suggested: confirmed `create_article`'s `tags[:4]` is still a silent slice with no schema constraint or accepted/dropped split, and confirmed `_gh`/`_dev`'s `except HTTPError` still forwards the raw response body unredacted (up to 400 chars) with no allowlist, no `retryable` flag, and no typed failure taxonomy — said so plainly instead of overclaiming coverage. One from `eduzsh` on the GET-only-guard post, asking whether the token itself got scoped down or just the code guard added — answered honestly: `GITHUB_TOKEN` is still full `repo, user` scope per `key_facts.md`, unchanged.
- Source: `drafts/comment_replies.md`.

---

### 2026-08-01 - DEV.to: 2 articles published (second run of the day)

- **Status**: Completed (both live; verified via HTTP 200 fetch of each URL)
- **Description**: Session started with HEAD detached from `main` again (same recurring provisioning symptom, `bugs.md` 2026-07-18), local `main` 2 commits behind `origin/main` — recovered with `scripts/sync-main.sh` before any git-writing work, fast-forwarded cleanly, no divergence. Quota check via live API showed 2 published today (this morning's nested-reply-audit / CLAUDE.md-gitignore pair), under the 5/day cap and non-zero, so the "second run, still 0 → publish at least 2" floor didn't apply — published 2 more per the normal 1-3 range, landing today's total at 4. Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments. Every top scorer was already excluded by prior runs' extensive logs: "The Junior Developer Pipeline Is Broken..." (r252 c201 — opinion/discuss shape, repeatedly flagged), "Slopsquatting..." (already mined 2026-07-30), "Unlimited context is not a feature" (too close to context-flooding/context-rot posts, repeatedly flagged), "If Your AI Agent Has Write Access to Public Repos" (already mined 2026-07-30), "My Verification Gate Cleared on a Keyword, Not Evidence" (self-distrust/verification vein, repeatedly flagged as overused), "I Connected 3 MCP Servers to One Agent" (single-MCP-server repo, credential-mixing reframe already mined 2026-07-28), "I Let an Agent Take Over an Account With Every Permission Check Green" (already mined 2026-07-27), "Your AI Agents Need Finite State Machines" (already mined 2026-07-30), "Too many Claude Code skills?..." (skipped again — tool-schema-bloat overlap risk). Checked two borderline trending items against this repo's actual code rather than dismissing on title alone: "MCP Went Stateless: Migrating to the 2026-07-28 Spec" doesn't apply — `server.py` runs over stdio (`mcp.run()`, no port/URL, launched as a subprocess per `key_facts.md`'s Claude Desktop config), not an HTTP server, so the article's entire premise (session headers, sticky routing, load balancers) has no target here; "What actually belongs in CLAUDE.md" maps cleanly onto this repo's own routing block but critiquing `CLAUDE.md`'s content is off-limits (user's own instructions file) and would be a third angle on a file already covered twice. No trending item cleared the distinct-angle bar, so per the task's quality bar, audited this repo's own current code instead — found two genuinely fresh, verified findings, both confirmed with real reproductions and fixed:
  1. `server.py`'s two HTTP helpers, `_gh()` (GitHub) and `_dev()` (DEV.to), had zero `try/except` around their `urllib.request.urlopen()` calls — every one of the 7 tools built on them (`list_articles`, `create_article`, `update_article`, `get_article_stats`, `get_github_profile`, `list_repos`, `get_repo_stats`) let any `HTTPError` (bad `article_id`/repo name, expired token, 422, 429) propagate as a raw uncaught exception, unlike `_claude()`'s three-times-hardened `ERROR:`-prefixed convention and unlike `publish_devto.py`'s own `except urllib.error.HTTPError` around the identical call shape. Verified live with `urlopen` stubbed to raise a 404: `update_article(article_id=999999)` and `get_repo_stats('doesnotexist')` both surfaced as `UNCAUGHT HTTPError propagated to caller`, no message content, in contrast to `_claude()`'s `'ERROR: claude -p exited 1: boom'`. Distinct from the 2026-07-28 "catches timeout, not non-zero exit" and "ERROR: convention" posts — both scoped to the `claude -p` subprocess call only, never touched `_gh`/`_dev`'s HTTP failure surface. Fixed: both helpers now catch `urllib.error.HTTPError` and raise `RuntimeError(f"... API error {e.code}: {e.read().decode()[:400]}")` instead of letting the raw exception through unlabeled. Reran the stubbed-404 repro against the fixed code — clean `RuntimeError` with status code and response body, not a bare `HTTPError` repr. Also fixed, same pass: `_gh`'s read-only guard checked the verb but let a `data` payload ride along on a `GET` call unrejected (surfaced in an unactioned 2026-08-01 comment reply) — guard now also rejects any non-`None` `data` on `_gh`.
  2. `create_article` never truncated `tags` to DEV.to's 4-tag limit, while its twin `publish_devto.py` (the script the scheduled publishing routine actually calls) already does (`[...][:4]`) — same repo, same endpoint, same rule enforced in exactly one of the two places that need it. Verified this wasn't just a stylistic diff: stubbed `urlopen` to capture the outgoing payload, called `create_article` with 6 tags — all 6 went out unmodified; ran the same 6 tags through `publish_devto.py`'s truncation logic in the same session — correctly cut to 4. Confirmed the 4-tag pattern is real and load-bearing by checking this log's own history: every one of ~30 prior published articles has exactly 4 tags, never more — an undocumented constraint encoded once, in one of two article-creation code paths, never checked against the other. Distinct from prior "twin function drifted" posts (system prompt string 2026-07-29, empty-diff guard 2026-07-24, `.env` path 2026-07-31) — same recurring shape, first instance of it being a numeric limit rather than a string or guard clause. Fixed: `payload["article"]["tags"] = tags[:4]`. Reran the payload-capture repro — `create_article` now sends exactly 4 tags regardless of input length. Did not extract a shared `_normalize_tags()` helper between the two files — this fix closes the one confirmed divergence; deduplicating on a single occurrence felt premature, left as an open smell noted in the article itself. Did not independently verify DEV.to's live API response to a 5-tag POST (422 vs. silent truncation) — the fix is correct regardless (it makes both code paths agree with the repo's own established pattern), but that specific verification gap is called out explicitly in the article rather than asserted as checked.
  - Tags: mcp, python, debugging, devtools; mcp, python, devtools, ai. Sources: `drafts/gh-and-dev-helpers-had-no-http-error-handling.md`, `drafts/create-article-never-truncated-tags.md`. Code changes: `server.py` (`_gh()`/`_dev()` now catch `HTTPError` and raise clean `RuntimeError`s; `_gh()` also rejects a `data` payload on GET; `create_article` truncates `tags` to 4).
  - https://dev.to/enjoy_kumawat/my-mcp-servers-two-api-helpers-had-zero-except-blocks-every-bad-call-crashed-with-a-raw-urllib-102n
  - https://dev.to/enjoy_kumawat/my-publish-script-truncates-tags-to-4-my-mcp-tool-that-does-the-same-job-never-learned-that-rule-59ao

---

### 2026-08-01 - DEV.to: 2 articles published (first run of the day)

- **Status**: Completed (both live; verified via HTTP 200 fetch of each URL)
- **Description**: Session started with HEAD detached from `main` again (same recurring provisioning symptom, `bugs.md` 2026-07-18), local `main` 1 commit behind `origin/main` — recovered with `scripts/sync-main.sh` before any git-writing work, fast-forwarded cleanly, no divergence. Quota check via live API showed 0 published today (today's only prior entry was a comment-reply-draft run, not a publish), so this was the first article-publishing run of the day; published 2 per the normal 1-3 range (no forced minimum — that clause only applies on the *second* daily run if the total is still 0). Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments. Every top scorer was already excluded by prior runs' extensive logs: "The Junior Developer Pipeline Is Broken..." (r251 c198 — opinion/discuss shape, repeatedly flagged), "Slopsquatting..." (already mined 2026-07-30), "I Connected 3 MCP Servers to One Agent" (already excluded multiple times — single-MCP-server repo, credential-mixing reframe mined 2026-07-28), "If Your AI Agent Has Write Access to Public Repos" (already mined 2026-07-30), "My Verification Gate Cleared on a Keyword, Not Evidence" (self-distrust/verification vein, repeatedly flagged as overused), "I Let an Agent Take Over an Account With Every Permission Check Green" (already mined 2026-07-27), "Your AI Agents Need Finite State Machines" (already mined 2026-07-30), "Too many Claude Code skills?..." (skipped again — tool-schema-bloat overlap risk), "Unlimited context is not a feature" (too close to the context-flooding/context-rot posts). "MCP Went Stateless: Migrating to the 2026-07-28 Spec" looked promising (a genuinely untouched angle — protocol version compliance) but this sandbox has no `mcp` package installed to check `server.py`'s actual transport against, so pursuing it would have meant speculating instead of verifying — dropped rather than force it. No trending item cleared the distinct-angle bar with something actually verifiable in this environment, so per the task's quality bar, audited this repo's own current git state and code instead — found two genuinely fresh, verified findings, both confirmed with real reproductions:
  1. Checked `.gitignore` against `git ls-files` and found `CLAUDE.md` listed as ignored (line 9) while also being fully tracked (`git ls-files CLAUDE.md` returns it). `git log --all -- CLAUDE.md` and `git log --all -- .gitignore` both point to the same origin commit (`29e0915`) — the file was added to the index and excluded by `.gitignore` in the same commit, which is why it's never actually been affected: tracked files bypass ignore rules by design. Verified the trap this leaves: `git check-ignore -v CLAUDE.md` (no flag) returns nothing/exit 1 — false reassurance, since check-ignore skips already-tracked paths — while `git check-ignore -v --no-index CLAUDE.md` correctly shows the pattern matches (`.gitignore:9:CLAUDE.md`). Reproduced the actual failure mode in a scratch clone: `git rm --cached CLAUDE.md`, delete, recreate with new content, `git add -A` — result is a staged deletion (`D  CLAUDE.md`) plus the new content sitting as untracked-and-ignored (`!! CLAUDE.md`, only visible with `git status --ignored`), never picked up by `git add -A`/`git add .` again without an explicit `-f`. Distinct from the 2026-07-31 CLAUDE.md post (that was about the routing-rules *content* asserting infrastructure that isn't attached; this is about the file's own git-tracking state being self-contradictory since its first commit, a completely different mechanism). Did not remove the `.gitignore` line or otherwise touch the file's tracking state — CLAUDE.md is the user's own instructions file, deciding its ignore/track status is their call, not a bug with an obvious one-line fix to auto-apply. Logged as a live but currently-dormant trap instead.
  2. Re-audited `reply_comments.py`'s `audit()` (unposted-reply detector, sibling to the `pending()`/`needs_reply()` pair fixed 2026-07-26) and found it checks only `c["children"]` (direct replies to the root comment) for a message from ME, while `needs_reply()`/`latest_message()` (fixed in the same 2026-07-26 pass, for a different bug) correctly recurse the whole subtree. A reply nested two levels deep — root comment → their follow-up → my reply to that follow-up, an ordinary back-and-forth shape — is invisible to `audit()`'s one-level check. Verified live with a constructed 3-level thread + a "drafted" marker for the root id_code: unfixed `audit()` reported it as `never_posted` while `needs_reply()` on the identical structure correctly returned `False`. Distinct from the 2026-07-26 fix itself (that fixed *which* message counts as "the reply" — recency vs. ever-replied — inside `needs_reply()`; this is a second function in the same file asking a simpler-sounding question, "did I reply at all," that never got the same recursive treatment, and had zero test coverage before today, unlike `needs_reply()`). Fixed: added `replied_anywhere_in_subtree()` (same recursive shape as `latest_message()`) and swapped it into `audit()` in place of the one-level `any(...)` check. Reran the identical repro against the fixed code — `never_posted` now correctly empty. Added a nested-reply case to the file's `--selftest` block, which previously only covered `needs_reply()`.
  - Tags: git, claudecode, devtools, debugging; python, debugging, devtools, ai. Sources: `drafts/claude-md-tracked-and-gitignored-same-commit.md`, `drafts/audit-only-checked-one-level-deep.md`. Code changes: `reply_comments.py` (`audit()` now uses new `replied_anywhere_in_subtree()` instead of a one-level `any()` check; `--selftest` covers a nested-reply case).
  - https://dev.to/enjoy_kumawat/my-claudemd-has-been-tracked-and-gitignored-since-the-commit-that-created-it-21ol
  - https://dev.to/enjoy_kumawat/my-comment-reply-audit-only-checked-one-level-deep-nested-replies-reported-as-never-posted-4hpc

---

### 2026-08-01 - DEV.to: 7 new comment replies drafted

- **Status**: Completed
- **Description**: `reply_comments.py pending` returned 7 comments across 5 articles. Two from `mads_hansen_27b33ebfee4c9` on the two most recent audits (`audit-log-that-leaves-no-trail`, `load-env-only-works-from-one-directory`) — checked their proposed fixes against current code before replying: confirmed the audit log still writes only on success (no `intent_recorded` step), and confirmed the missing-credential path is still a bare `KeyError` from inside `_gh()`/`_dev()`, not a startup-time report. One from `mads_hansen_27b33ebfee4c9` on the GET-only-helper post — checked current `server.py`: the `method != "GET"` guard does raise before any `urlopen` call (verified via the code, matches their first ask), but a `data` payload attached to a `GET` call isn't rejected (their second ask) and `GITHUB_TOKEN` is still the full `repo, user` scope, not narrowed — said so plainly instead of claiming both were covered. Two from `alexshev` (short, agreeing comments on the state-machine and slopsquatting posts) got proportionately short replies. Two on "My MCP Server Holds Two API Keys" (still unsplit — verified `server.py` is one process, one `FastMCP` instance, `load_env()` still loads both credentials at import): `fern_eterna` asked a legitimate threat-model question while also linking their own product (`mcp.eterna.exchange/mcp`) — answered the technical question, stayed noncommittal about the tool; `rizzdev` raised a real hole in the article's own proposed fix (an MCP client aggregates both servers' tools into one list for the agent, and no origin tag travels with a tool call, so a two-process split doesn't stop one conversation from using both servers' tools back to back) — agreed it's correct and said I don't have an answer for it yet, rather than defending the original framing.
- Source: `drafts/comment_replies.md`.

---

### 2026-07-31 - DEV.to: 2 articles published (second run of the day)

- **Status**: Completed (both live; verified via HTTP 200 fetch of each URL)
- **Description**: Session started with HEAD detached from `main` again (same recurring provisioning symptom, `bugs.md` 2026-07-18), local `main` 19 commits behind `origin/main` — recovered with `scripts/sync-main.sh` before any git-writing work, fast-forwarded cleanly, no divergence. Quota check via live API showed 2 published today (this morning's `.env`-loader / CLAUDE.md-routing pair), under the 5/day cap and non-zero, so the "second run, still 0 → publish at least 2" floor didn't apply — published 2 more per the normal 1-3 range, landing today's total at 4. Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments. Every top scorer was already excluded by prior runs' extensive logs: "The Junior Developer Pipeline Is Broken..." (r238 c191 — opinion/discuss shape, repeatedly flagged), "Slopsquatting..." (already mined 2026-07-30), "I Let an Agent Take Over an Account With Every Permission Check Green" (already mined 2026-07-27), "I Connected 3 MCP Servers to One Agent" (single-MCP-server repo, credential-mixing reframe already mined 2026-07-28), "Your AI Agents Need Finite State Machines" (already mined 2026-07-30), "If Your AI Agent Has Write Access to Public Repos" (already mined 2026-07-30), "My Verification Gate Cleared on a Keyword, Not Evidence" (self-distrust/verification vein, repeatedly flagged as overused), "Too many Claude Code skills?..." (skipped again — same tool-schema-bloat overlap risk as this morning). No trending item cleared the distinct-angle bar, so per the task's quality bar, audited this repo's own current code instead — found two genuinely fresh, verified findings, both confirmed with real reproductions rather than by rereading the code:
  1. Re-examined `hooks/prepare-commit-msg`, the subject of three prior posts (timeout handling 2026-07-23, "never installed as a real git hook" 2026-07-26, "install didn't survive a fresh container" 2026-07-27) — but none of those three ever verified the hook actually produces an AI commit message end-to-end once installed. It doesn't, and never has: `SCRIPT="$(cd "$(dirname "$0")/.." && pwd)/git_commit.py"` was written assuming `$0` resolves from this file's tracked location (`<repo>/hooks/`), where one `..` reaches the repo root. But git only ever invokes hooks from `.git/hooks/` (per `scripts/install-hooks.sh`'s flat copy) — one directory deeper — so the same one `..` lands in `.git/`, not the repo root, and `SCRIPT` points at `<repo>/.git/git_commit.py`, which has never existed in this repo's history. Verified live: copied the real working tree to a scratch clone, ran the real `scripts/install-hooks.sh`, did a real `git commit` with a working `claude` CLI on PATH (so this isn't the missing-binary case either) — `python .git/git_commit.py` fails with `FileNotFoundError`, silently eaten by the existing `2>/dev/null`, `MSG` stays empty, and `git commit` aborts on an empty message, indistinguishable from "the hook isn't installed at all." Fixed: changed to `dirname "$0")/../..` (two levels from `.git/hooks/` to repo root). Reran the identical repro against the fixed hook — a real Conventional Commit message, generated by `claude -p`, landed in the test commit for the first time since this hook was written.
  2. Audited whether the JSONL audit log the 2026-07-27 `update_article` fix added (`logs/article_updates.jsonl`, built specifically so a bad write would leave a trace) actually persists anywhere. It doesn't: `git log --all --oneline -- 'logs/*'` is empty across all 50 commits (repo not shallow, confirmed via `git rev-parse --is-shallow-repository`), `logs/` isn't in `.gitignore` either — it's simply never been part of any committed state, and doesn't exist on disk right now. Root cause: `update_article` is an MCP tool invoked wherever a client has this server wired up (per `key_facts.md`, that's Claude Desktop on the user's own machine), while the scheduled publishing routine that writes these articles is a separate, fresh-per-session cloud container that never calls `update_article` at all (it publishes via `publish_devto.py` directly) — so the one tool this log exists for writes a plain local file with no step anywhere in this repo's workflow that commits it somewhere both environments could read. Checked whether `scripts/check_key_facts.py` (built to catch exactly this class of docs/disk drift, fixed twice already for missing-from-docs and phantom-in-docs script rows) would ever catch this: it only walks `ROOT`, `scripts/`, `hooks/` for `.py`/`.sh` files — a runtime JSONL log in an undocumented `logs/` directory is categorically outside what it looks for. Did not force a fix — same restraint as the 2026-07-19 stop-hook post: committing a growing audit log forever has the same downside ADR-005 already rejected for `drafts/`, and picking the right fix (shared log location vs. explicitly scoping the tool's durability claim) is a design decision, not a five-minute patch. Logged as an open gap instead.
  - Tags: git, python, claudecode, debugging; mcp, python, devtools, debugging. Sources: `drafts/commit-hook-never-found-its-own-script.md`, `drafts/audit-log-that-leaves-no-trail.md`. Code changes: `hooks/prepare-commit-msg` (`SCRIPT` path now correctly resolves two directories up from the installed `.git/hooks/` location, not one).
  - https://dev.to/enjoy_kumawat/my-ai-commit-hook-got-a-timeout-fix-and-an-install-fix-it-had-never-once-actually-run-45pa
  - https://dev.to/enjoy_kumawat/my-mcp-tools-audit-log-was-built-so-a-bad-write-would-leave-a-trace-the-log-itself-leaves-none-4maf

### 2026-07-31 - DEV.to: 2 articles published (first run of the day)

- **Status**: Completed (both live; verified via HTTP 200 fetch of each URL)
- **Description**: Session started with HEAD detached from `main` again (same recurring provisioning symptom, `bugs.md` 2026-07-18), local `main` 18 commits behind `origin/main` — recovered with `scripts/sync-main.sh` before any git-writing work, fast-forwarded cleanly, no divergence. Quota check via live API showed 0 published today (new UTC day; last entry was 2026-07-30's second run), so this was the first article-publishing run of the day; published 2 per the normal 1-3 range (no forced minimum — that clause only applies on the *second* daily run if the total is still 0). Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments. Every top scorer was already excluded by prior runs' extensive logs or off-voice: "The Junior Developer Pipeline Is Broken..." (opinion/discuss shape, repeatedly flagged), "The Person Who Fixed the Bugs Just Vanished" (no clean first-hand hook, excluded before), "Slopsquatting..." (already mined 2026-07-30), "I Connected 3 MCP Servers to One Agent" (single-MCP-server repo, its credential-mixing reframe already mined 2026-07-28), "I Let an Agent Take Over an Account With Every Permission Check Green" and "My Verification Gate Cleared on a Keyword, Not Evidence" (self-distrust/verification vein, repeatedly flagged as overused), "Your AI Agents Need Finite State Machines" and "If Your AI Agent Has Write Access to Public Repos" (already mined 2026-07-30), "Too many Claude Code skills? How the listing budget decides..." (close enough to the 2026-07-03 tool-schema-bloat post to risk overlap — skipped rather than force it). No trending item cleared the distinct-angle bar, so per the task's quality bar, audited this session's own environment and this repo's own code instead — found two genuinely fresh, verified findings:
  1. This repo's own `CLAUDE.md` opens with a section titled "MANDATORY routing rules" asserting a `context-mode` MCP server with tools (`ctx_fetch_and_index`, `ctx_execute`, `ctx_batch_execute`, `ctx_search`, `ctx_index`) that block/redirect `curl`, `WebFetch`, and large Bash/Grep output. This run's own environment notes told me to ignore that section entirely — those tools don't exist in this sandbox — confirmed independently by checking the session's actual connected-tool list at startup: zero `ctx_*` tools present, only `mcp__github__*`/`mcp__Context7__*`/`mcp__Indeed__*`/native CLI tools. Distinct from every prior context-mode/context-flooding post (the 2026-06-23 "56 KB" post and the 2026-06-24 memory post both took the tooling's *existence* for granted and wrote about its runtime behavior; this is the first to note the routing file asserts infrastructure with no mechanism to check whether it's actually attached to a given session, and that only a session-specific override note — not the file itself — kept a literal reading of "MANDATORY"/"do NOT retry" from sending an unattended run down a dead-end tool-not-found path). Also flagged, but did not act on, a second unverifiable assertion in the same file ("codex will review what you done" — no corresponding hook/config/mention anywhere in this repo). Did not edit `CLAUDE.md` this run — same restraint as the 2026-07-19 stop-hook post: a standing instructions file is the user's call to change, not a code bug with a before/after diff to verify against.
  2. Audited `server.py`'s `load_env()` against its near-twin in `publish_devto.py` (this repo's established pattern of copy-drift: commit-prompt twin 2026-07-29, `_claude()`-timeout twin 2026-07-24). Found `server.py`'s version opened the literal relative string `".env"` — dependent on the process's cwd at launch — while `publish_devto.py`'s already resolves `.env` relative to its own file path. Verified this is a live gap, not a hypothetical: `key_facts.md`'s own documented Claude Desktop config for this server sets `command`+`args` only, no `cwd`, and reproduced offline (clean `env -i` process, `chdir("/tmp")` before calling `load_env()`) that `GITHUB_TOKEN`/`DEV_TO_API` silently fail to load from an unrelated cwd — the `FileNotFoundError` is caught and swallowed, so the first visible symptom is a bare, unhandled `KeyError` inside `_gh()`/`_dev()`, bypassing this repo's own `ERROR:`-prefixed failure convention (`_claude()`'s three except clauses) entirely, because the failure happens two calls upstream of where that convention was ever applied. Fixed: `load_env()` now resolves `.env` relative to `os.path.dirname(os.path.abspath(__file__))`, matching `publish_devto.py`. Reran the same repro against the fixed function from `cwd=/tmp` — both keys now load correctly. Did not add friendlier error messages for a missing credential beyond the path fix — a separate, unreproduced gap, left for another run.
  - Tags: claudecode, mcp, ai, devtools; python, mcp, debugging, devtools. Sources: `drafts/claude-md-mandatory-rules-server-never-attached.md`, `drafts/load-env-only-works-from-one-directory.md`. Code changes: `server.py` (`load_env()` now resolves `.env` relative to the file's own directory, not cwd).
  - https://dev.to/enjoy_kumawat/my-claudemd-calls-its-routing-rules-mandatory-the-mcp-server-behind-them-was-never-attached-3kif
  - https://dev.to/enjoy_kumawat/my-mcp-servers-env-loader-only-works-if-you-launch-it-from-one-specific-directory-my-mcp-client-oc7

---

### 2026-07-30 - DEV.to: 2 articles published (second run of the day)

- **Status**: Completed (both live; verified via HTTP 200 fetch of each URL)
- **Description**: Quota check via live API showed 1 published today (this morning's `_gh` GET-only-guard post), well under the 5-article cap and above zero, so the "second run, still 0 → publish at least 2" floor didn't apply — published 2 more per the normal 1-3 range, landing today's total at 3. Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments. Several top scorers were already excluded by this morning's run's log or prior runs: "I Connected 3 MCP Servers to One Agent," "I Let an Agent Take Over an Account With Every Permission Check Green," "My Verification Gate Cleared on a Keyword, Not Evidence," "If Your AI Agent Has Write Access to Public Repos" (source of this morning's post); "Unlimited context is not a feature" was skipped as too close to the existing context-flooding/context-rot posts. Picked 2 themes with a distinct first-hand angle not covered by the prior 31 posts:
  1. "Slopsquatting: The Supply Chain Attack That Weaponizes AI Hallucinations" trend (security/ai, score 227 — highest of the run) → reframed around a real fact found by checking this repo's own `requirements.txt` (one pinned dependency, `mcp[cli]`) and `decisions.md`'s ADR-001/ADR-002 (both rejected `requests`/`httpx` purely for install friction, not security). The insight: a friction-reduction decision to stay stdlib-only, made in 2026-06-21 for convenience, incidentally eliminates the entire "agent hallucinates a plausible package name, attacker already registered it" attack surface, because there's no `pip install` code path in the routine for a bad suggestion to reach. Distinct from all prior MCP-security posts (those were about vetting/scoping *MCP servers and their credentials*; this is about *dependency-install* trust, a different mechanism entirely, and the first post to touch supply-chain/typosquatting at all). Included a concrete PyPI-existence-check snippet for projects that do need to add dependencies on an agent's suggestion.
  2. "Your AI Agents Need Finite State Machines (FSMs)" trend (agents, score 69) → tested the claim against this very routine's own prose-numbered Step 1-5 structure, which has no persisted state file and runs in a container that gets reclaimed between runs. Found the FSM argument didn't actually require new code here: Step 1's live quota check makes dev.to's own `/me/published` the single source of truth for "what's actually live," so a crash between Step 3 (draft written) and Step 4 (publish) can't cause a double-publish — the next run only trusts what dev.to confirms, never a local record of what a previous run believed it did. Also surfaced the one place local state *does* matter (the issues.md log entry, if a partial multi-article publish gets logged as fully successful) and pointed at Step 4's existing "verify each article is live by fetching its URL" instruction as the real state-transition guard (`attempted -> confirmed_live`) already doing that job in prose. Distinct from the FSM/verification-discipline posts already published (those were about tests/tool-results self-reporting; this is specifically about *where* durable state should live across a crash-prone unattended run).
  - Tags: security, python, ai, devtools; ai, agents, claudecode, devtools. Sources: `drafts/one-dependency-and-zero-pip-installs.md`, `drafts/publishing-agent-state-machine.md`.
  - **Also fixed a real pipeline bug found while running Step 5 of this run**: `.gitignore` had `drafts/*` / `!drafts/comment_replies.md`, which silently excluded every article draft from `git add` — confirmed via `git log --diff-filter=A -- 'drafts/*.md'` that no article draft has ever actually been committed despite dozens of prior log entries citing `drafts/<slug>.md` as a "Source." Changed the negation to `!drafts/*.md` so article drafts commit going forward, same as `comment_replies.md` always did.
  - https://dev.to/enjoy_kumawat/my-repo-has-one-pinned-dependency-and-zero-pip-install-calls-i-didnt-design-that-for-security-5aal
  - https://dev.to/enjoy_kumawat/i-looked-for-where-my-publishing-agent-needed-a-state-machine-the-state-machine-was-already-live-ddh

### 2026-07-30 - DEV.to: 1 article published (first run of the day) — quality bar, not forcing more

- **Status**: Completed (live; verified via HTTP 200 fetch of the URL)
- **Description**: Session started with HEAD detached from `main` again (same recurring provisioning symptom, `bugs.md` 2026-07-18), local `main` 16 commits behind `origin/main` — recovered with `scripts/sync-main.sh` before any git-writing work, fast-forwarded cleanly, no divergence. Quota check via live API showed 0 published today and no first-run log entry yet today, so this was the first article-publishing run of the day. Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments. Every top scorer was already excluded by prior runs' extensive logs or off-voice: "The Junior Developer Pipeline Is Broken...", "The Dirty Secret Behind AI Agents (Demo)", "The Person Who Fixed the Bugs Just Vanished" (all repeatedly flagged discuss/marketing/off-voice), "How I reduced AI coding context by 95%" (too close to the context-flooding/context-mode vein), "I Connected 3 MCP Servers to One Agent" (single-MCP-server repo, its credential-mixing reframe already mined 2026-07-28), "My AI pentest agent reported 23 root shells..." (faked-tool-result vein, already mined), "I Let an Agent Take Over an Account With Every Permission Check Green" (already mined 2026-07-27), "My Verification Gate Cleared on a Keyword, Not Evidence" (self-distrust/verification vein, repeatedly flagged as overused). One trending item did survive first-pass filtering — "If Your AI Agent Has Write Access to Public Repos, Audit It Now" (ai/security, r36 c14) — and rather than dismiss it on the "no write tools here" reflex, went and read the actual GitHub helper function instead of just the tool list. Found a real, previously-undocumented gap: `server.py`'s `_gh(path, method="GET", data=None)` is a fully generic HTTP client (accepts any verb, serializes and attaches `data` if given) — grepped every call site (`get_github_profile`, `list_repos`, `get_repo_stats`) and confirmed all three pass only the default `GET` with no `data`, so the "read-only" story was true by convention only, never enforced. That gap is sharper than it looks because of what actually backs the call: `key_facts.md` documents the real token's scope as `repo, user` — full read/write across every repo the account can reach, not read-only. The token doesn't know only reads are happening; until this run, neither did the function. Distinct from the 2026-07-28 "two API keys share one process" post (that was about *two different credentials* sharing a process, no fix shipped) and the 2026-07-22 GitHub-proxy-403 post (a sandbox artifact, different mechanism entirely) — this is about *one* credential having reach far beyond what any current tool needs, with nothing in code resisting a stray write call from a future tool, a bad edit, or an over-eager agent. Fixed this run: added a `method != "GET"` guard to `_gh` that raises `ValueError` before any request is constructed. Verified two ways with `urlopen` stubbed (no dependency on this sandbox's own known GitHub-proxy quirks): the three existing read tools still make exactly the same `GET` calls and return the same shapes; a stray `_gh(path, method="DELETE")` now raises immediately with zero network calls made, confirmed via a call counter.
- Tags: mcp, security, python, devtools. Source: `drafts/gh-helper-could-write-nothing-used-it-that-way.md`. Code changes: `server.py` (`_gh` now rejects any non-`GET` method).
- https://dev.to/enjoy_kumawat/my-mcp-servers-github-helper-function-could-post-and-delete-every-tool-that-called-it-only-ever-kjb

---

### 2026-07-30 - DEV.to: drafted replies for 4 new comments

- **Status**: Completed
- **Description**: `reply_comments.py pending` returned 4 comments across 4 articles. Drafted all 4: alexshev on the `update_article` diff piece (confirmed the diff still has no destructive-vs-cosmetic flag — just before/after values for whatever changed), alexshev on the phantom-files memory piece (confirmed `check_key_facts.py` only covers `key_facts.md`'s Project Files table, not `decisions.md`/`issues.md`, both of which also asserted the same phantom scripts), alexshev on the `claude -p` wrapper piece (confirmed the three except clauses are still one flat `ERROR:`-prefixed string, no typed taxonomy, and empty/partial output aren't checked at all), rulestack on the commit-hook timeout piece (confirmed `hooks/prepare-commit-msg` still redirects `2>/dev/null` with no log line, so a silent fallback is still indistinguishable from "nothing to prefill").
- Source: `drafts/comment_replies.md`.

---

### 2026-07-29 - DEV.to: 1 article published (second run of the day)

- **Status**: Completed (live; verified via HTTP 200 fetch of the URL)
- **Description**: Session started with HEAD detached from `main` again (same recurring provisioning symptom, `bugs.md` 2026-07-18) with local `main` 14 commits behind `origin/main` — recovered with `scripts/sync-main.sh` before any git-writing work, fast-forwarded cleanly. Quota check via live API showed 2 published today (first run's pair), under the 5 cap and non-zero, so the "publish ≥2 if still 0 on second run" clause didn't apply — normal 1-3 range. Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments; every strong scorer either overlapped a topic mined in the last 1-2 days ("I Connected 3 MCP Servers to One Agent" vs. 2026-07-28's single-process-two-keys post; pentest/verification-gate posts vs. the already-overused faked-tool-result/self-distrust vein) or was off-voice (marketing/discuss-shape posts excluded in prior runs' logs too). No trending item cleared the distinct-angle bar, so per the task's quality bar went back to this repo's own unmined bug log and found one genuinely fresh, unarticled entry: the 2026-07-19 `bugs.md` item where a post-publish stop hook (`~/.claude/stop-hook-git-check.sh`) flagged 8 pushed commits as GitHub-"Unverified" and prescribed `git config user.name Claude` + `--amend --reset-author`/`rebase --exec` across already-pushed shared history. Verified the hook's own diagnosis against raw data (`git log --format='%h %an <%ae> / %cn <%ce>'`) rather than trusting it: 6/8 commits already had the correct committer, the actual gap was a missing GPG/SSH signature (not identity), so the prescribed identity-reset fix would have targeted the wrong root cause entirely. Separately, the prescribed remedy crossed two hard lines regardless: force-rewriting already-pushed shared history with no unattended authorization, and setting committer identity to "Claude" being AI attribution moved from commit-message text into git metadata — a direct collision with this repo's own no-AI-attribution convention (previously covered in the 2026-07-15 "kept signing its own work" post, a different mechanism: that was the message-generator adding attribution text, this is a hook asking to encode it in identity fields instead). Distinct from all detached-HEAD/git-hook posts published so far (2026-07-15 worktree misdiagnosis, 2026-07-18 recurrence, 2026-07-26/27 hook-installation gap) — none of those touch commit signing, GitHub verification badges, or a hook prescribing history rewrites. Did not run the hook's fix; left the badges as-is and logged it as a decision deferred to the user.
  - Tags: git, ai, claudecode, security. Source: `drafts/stop-hook-told-me-to-rewrite-history.md`.
  - https://dev.to/enjoy_kumawat/my-repos-own-safety-hook-found-8-unverified-commits-and-told-me-to-rewrite-them-3b9g

### 2026-07-29 - DEV.to: 2 articles published (first run of the day)

- **Status**: Completed (both live; verified via HTTP 200 fetch of each URL)
- **Description**: Session started with HEAD detached from `main` again (same recurring provisioning symptom, `bugs.md` 2026-07-18), local `main` 13 commits behind `origin/main` — recovered with `scripts/sync-main.sh` before any git-writing work, fast-forwarded cleanly, no divergence. Quota check via live API showed 0 published today (new UTC day; last entry was 2026-07-29's comment-reply run), so this was the first article-publishing run of the day; published 2 per the normal 1-3 range. Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments. Every top scorer was already excluded by prior runs' logs or off-voice: "The Dirty Secret Behind AI Agents (Demo)" (marketing tone, excluded repeatedly), "The Junior Developer Pipeline Is Broken... And AI Broke It" (discuss/opinion shape, off-voice), "The Person Who Fixed the Bugs Just Vanished" (no clean first-hand hook, excluded before), "I Connected 3 MCP Servers to One Agent" (single-MCP-server repo, no genuine multi-server angle, its one reframe already mined 2026-07-28), "How I reduced AI coding context by 95%" (too close to the context-flooding/context-mode vein), "I Let an Agent Take Over an Account With Every Permission Check Green" (already mined for the 2026-07-27 blind-write post), "My AI pentest agent reported 23 root shells..." (faked-tool-result vein, already mined). Two borderline candidates ("My Verification Gate Cleared on a Keyword, Not Evidence," "You Recorded the Incident. Now Prove Your Fix Actually Works.") both risked landing back in the self-distrust/verification-audit vein already flagged as overused. No trending item offered a genuinely fresh, repo-grounded angle, so per the task's own quality bar, went back to auditing this repo's own current code — found two, both verified via git archaeology / offline repro and fixed this run:
  1. The 2026-06-21 comment "fix: strip AI attribution lines from generated commit messages" (`1f8808c`) touched both `git_commit.py` and `server.py` in the same commit, but not the same way: it strengthened `git_commit.py`'s system prompt with an explicit "one line, ... no co-author lines, no signatures, no AI references" instruction, while `server.py`'s `generate_commit_message` tool only got the shared `_STRIP` filter added to `_claude()` — its own system prompt argument was never touched, and still read the old, unstrengthened text five weeks and two regex-hardening passes (2026-07-22, 2026-07-26) later. Verified via `git log --reverse -p` on both files to pin the exact fork point, then confirmed live: the shared `_STRIP_RE` regex returns `False` (i.e. does not catch) synthetic model output like `"As an AI language model, I cannot take credit for this commit."` — exactly the class of phrasing `git_commit.py`'s extra instruction is designed to prevent at the source, and which `server.py`'s tool had zero defense against beyond the same downstream regex. Distinct from the 2026-06-21/2026-07-19 "same code" framing (that was about the shared `_claude()` helper and STRIP filter being identical, which they are) and the 2026-07-22/2026-07-26 STRIP-filter posts (both about the regex itself) — this is about the instruction layer upstream of the filter, never diffed in four separate touchpoints on this exact function pair. Fixed: `server.py`'s `generate_commit_message` system prompt now matches `git_commit.py`'s verbatim. Verified via `inspect.getsource` that the string now contains the same instruction.
  2. Re-audited the 2026-07-27 `update_article` fetch-before-write fix (the one that added diff/log to close the blind-overwrite gap) and found its own diff/log only ever covered `title`/`published`, hardcoded, regardless of which field the caller actually changed — `body_markdown`, the field this tool exists to edit, was never in either. Verified offline (stubbed before/after states, no live article touched, same caution as the original fix): calling `update_article` with only `body_markdown` set produced a diff showing `title` and `published` both unchanged, with no field anywhere reflecting that the entire body had just been replaced; the JSONL log recorded `fields_changed: ["body_markdown"]` but no `body_markdown_before`/`after` values, so even the audit trail couldn't show what changed. Distinct from the 2026-07-27 post itself (found the tool had no diff/log at all) — this is the fix for that gap having a coverage gap of its own, caught only by asking which field a real call is actually likely to touch rather than trusting that a `diff` key existing meant the diff was complete. Fixed: `diff` is now built from `article.keys()` (whatever fields were actually part of the write) instead of a fixed pair, and `_log_article_update` now logs before/after only for fields in `fields_changed`, covering `body_markdown` for the first time. Reran the same stubbed scenario against the fixed code — diff and log both now show the actual old/new body text.
  - Tags: mcp, python, ai, debugging; mcp, security, python, devtools. Sources: `drafts/commit-generator-twin-never-got-the-prompt-fix.md`, `drafts/update-article-diff-never-diffed-the-body.md`. Code changes: `server.py` (`generate_commit_message` system prompt now matches `git_commit.py`'s; `update_article`'s `diff` and `_log_article_update` now cover only-and-all actually-changed fields, including `body_markdown`).
  - https://dev.to/enjoy_kumawat/my-commit-script-was-fixed-to-never-say-as-an-ai-my-mcp-tools-copy-of-the-same-function-wasnt-347f
  - https://dev.to/enjoy_kumawat/i-fixed-my-mcp-tool-to-diff-before-overwriting-an-article-the-diff-never-looked-at-the-body-531c

### 2026-07-29 - DEV.to: drafted replies for 4 new comments

- **Status**: Completed
- **Description**: `reply_comments.py pending` returned 4 comments across 2 articles. Three (`mads_hansen_27b33ebfee4c9`, `max_quimby`, `zira125`) landed on "My MCP Server Holds Two API Keys," each pushing past the two-process split that article sketches and stopped short of applying — toward per-tool capability injection, brokered short-lived credentials, and adversarial boundary fixtures. Grounded all three in the actual current state of `server.py`: still one process, `load_env()` still loads both `GITHUB_TOKEN` and `DEV_TO_API` into the same `os.environ` at import, no broker, no fixture, no audit trail — none of what's proposed has been built. The fourth (`neelagiri65`, on "I Gave My MCP Tool an ERROR: Convention") asked whether the taught/untaught failure-path gap was one-off; answered from the very next article in this log (`d91ebc9`), which found a third failure path (`CalledProcessError`, `FileNotFoundError`) days after the first "fix" declared the convention settled — confirms it wasn't one-off, and there's still no exhaustive failure-mode spec, just incidents patched as found.
- Source: `drafts/comment_replies.md`.

---

### 2026-07-28 - DEV.to: 1 article published (second run of the day) — quality bar, not forcing more

- **Status**: Completed (live; verified via HTTP 200 fetch of the URL)
- **Description**: Live quota check showed 2 already published today (first run) — total 3 after this run, within the 5/day cap, no forced minimum since today wasn't still at 0. Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments. Top scorers were mostly repeats already excluded across prior runs' logs: "The Dirty Secret Behind AI Agents (Demo)" (marketing tone, excluded repeatedly), "How I reduced AI coding context by 95%" (too close to the long-mined context-flooding/context-mode vein), "My AI pentest agent reported 23 root shells..." (faked-tool-result vein, already mined), "Pin your MCP server contracts..." (near-identical to the 2026-07-23 unpinned-contract post). One candidate the 2026-07-27 run had dismissed outright — "I Connected 3 MCP Servers to One Agent. It Got Scary Fast." (mcp, r12 c16), logged then as "already excluded (single-MCP-server repo, no genuine angle)" — got a second look, because that dismissal took the post's literal framing (3 separate servers) at face value instead of the underlying risk (a single agent session holding the union of every credential reachable from it). Re-audited `server.py` under that reframing rather than the surface one: it's one MCP server, but it loads both `GITHUB_TOKEN` and `DEV_TO_API` into the same process at import time (`load_env()`), and every one of its 8 tools — including `generate_commit_message`, which touches neither API — runs in that same process with both credentials sitting in its environment. That's a genuine angle the prior run's framing missed: the "3 servers vs 1" distinction doesn't matter: what matters is whether tool-surface separation matches credential separation, and in this repo's actual code it doesn't. Verified this isn't a rehash of any prior post: grepped `issues.md` for prior coverage of credential/process-boundary framing — the closest hits were the 2026-07-14 "no logging on `_gh`/`_dev`" post (about missing observability, not credential scope) and a comment-reply aside about `_gh`/`_dev` having no origin-vs-policy distinction (not about the two credentials sharing a process). No prior article argues the actual point here: that single-process credential mixing is the same failure shape as the trending multi-server post, just harder to see because there's no second wire to point at. Did not modify `server.py` this run — the article proposes and sketches a two-process split (separate `.env`s, separate `FastMCP` instances per credential domain) as the fix but stops short of applying it, stated as such in the piece itself.
- Tags: mcp, security, ai, agents. Source: `drafts/one-mcp-server-two-credentials.md`.
- https://dev.to/enjoy_kumawat/my-mcp-server-holds-two-api-keys-every-tool-call-runs-in-the-same-process-as-both-58a9

### 2026-07-28 - DEV.to: 2 articles published (first run of the day)

- **Status**: Completed (both live; verified via HTTP 200 fetch of each URL)
- **Description**: Session started with HEAD detached from `main` again (same recurring provisioning symptom, `bugs.md` 2026-07-18), local copy 10 commits behind `origin/main` — recovered with `scripts/sync-main.sh` before any git-writing work, fast-forwarded cleanly, no divergence. Quota check via live API showed 0 published today (new UTC day; last entry was 2026-07-27's comment-reply run), so this was the first article-publishing run of the day; published 2 per the normal 1-3 range. Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments. Every top scorer was already excluded by prior runs' logs or off-voice: "The Dirty Secret Behind AI Agents (Demo)" (marketing-tone, excluded repeatedly), "The Person Who Fixed the Bugs Just Vanished" (no clean first-hand hook, excluded before), "The Junior Developer Pipeline Is Broken... And AI Broke It" (opinion/community-discuss shape, off-voice), "Loop Engineering: Stop Your Agent Reward-Hacking Its Own Checks" (repeatedly flagged too close to the self-distrust/verification vein), "Sentry's Span Hierarchy Exposed a Silent Retry in My 5-Agent Pipeline" (this repo runs one MCP server, not a 5-agent pipeline — no genuine angle), "I Let an Agent Take Over an Account With Every Permission Check Green" (same "check passed, outcome still bad" shape already mined for the 2026-07-27 `update_article` blind-write post). No trending item offered a genuinely distinct, repo-grounded angle, so per the task's own quality bar, went back to auditing this repo's own current code instead of forcing a trend match — found two, both verified live and fixed this run:
  1. The 2026-07-26 post ("I Gave My MCP Tool an ERROR: Convention...") fixed a mismatch between `_claude()`'s two *returning* failure paths (empty-diff guard, timeout) so both carried an `ERROR:` prefix, and treated the convention as settled. It wasn't: `subprocess.check_output(["claude", "-p", ...])` also raises `subprocess.CalledProcessError` on a non-zero exit and `FileNotFoundError` if the binary is missing from `PATH` — neither is a `TimeoutExpired`, so neither was caught by the sole `except subprocess.TimeoutExpired` clause in either `server.py`'s `_claude()` or `git_commit.py`'s inline call. Verified live: dropped a one-line stand-in `claude` binary onto `PATH` that exits 1, ran the real staged-diff path through `git_commit.py`, got a full uncaught `CalledProcessError` traceback (exit 1) instead of any clean message; repeated with `PATH` emptied of `claude` entirely and got an uncaught `FileNotFoundError` the same way; reproduced both against `server.py`'s actual `_claude()` by stubbing the `mcp` import so the real module could be loaded and called directly. The commit-hook path is saved from user-visible damage only by an accident (`hooks/prepare-commit-msg` redirects `2>/dev/null` and only writes to the commit-message file on non-empty stdout, so the traceback is silently discarded and it falls back to git's own template) — the MCP tool path has no equivalent safety net; an uncaught exception there is just an uncaught exception. Distinct from the 2026-07-26 post: that one found two return statements disagreeing on formatting; this is a third code path that was never a return statement at all, live in the same six-line `try/except` block. Fixed: added `except subprocess.CalledProcessError` (with `stderr=subprocess.PIPE` on the call so the message can include the actual failure reason) and `except FileNotFoundError` to both `server.py`'s `_claude()` and `git_commit.py`, mirroring the existing `ERROR:`-prefixed / stderr-printed pattern each file already used for timeouts. Reran both repro cases against the fixed code — clean `ERROR: claude -p exited 1: simulated failure: rate limited` and `ERROR: claude CLI not found on PATH`, no tracebacks.
  2. Re-audited `scripts/check_key_facts.py` (built 2026-07-25 to catch `key_facts.md`'s Project Files table going stale) and found it only checks one direction — real files not mentioned in the docs — never the reverse. Checked whether the reverse direction had any live cases: `key_facts.md` documents `update_profile.py` ("Pushes `template.md` to GitHub profile README") and `template.md` ("Source of truth for GitHub profile README"), neither of which exists on disk. Confirmed this isn't a stale-after-deletion case (unlike `post_article.py`, which `key_facts.md` correctly documents as removed 2026-07-16): `git rev-parse --is-shallow-repository` is `false`, `git rev-list --all --count` is 44, and `git log --all --diff-filter=A --name-only -- update_profile.py template.md` returns nothing — neither file was ever committed to this repo, in any commit, ever. Yet `docs/project_notes/issues.md`'s 2026-06-21 entry claims "Created `template.md`... Auto-push via `update_profile.py`," and `decisions.md` has an ADR reasoning about `update_profile.py`'s design. Root cause of what actually happened outside this repo is unknowable from inside it; what's verifiable is narrower and still real — `key_facts.md`, the file this repo's own agents are told to trust instead of re-deriving "what scripts exist," asserted two working tools that a full, non-shallow history search proves were never part of the codebase. Distinct from the 2026-07-25 post that built this checker (that found real files with no docs — missing-from-docs) — this is the mirror bug (docs claiming a phantom file), worse in effect for an agent that trusts the file: a missing-from-docs script is invisible until needed; a phantom-in-docs script gets confidently referenced and only then found not to exist. Fixed: added `project_files_table_rows()` (isolates just the Project Files table's first column, not every backticked token in the file, since other rows/prose reference API paths and env var names that aren't filenames) and a second check in `main()` for table rows whose target doesn't exist on disk, with an explicit `.env` exception (documented as intentionally never committed, so its absence is by design). First run against the unfixed docs correctly flagged both phantom rows; removed them from `key_facts.md`; reran clean.
  - Tags: mcp, python, debugging, devtools; ai, python, devtools, debugging. Sources: `drafts/claude-p-wrapper-catches-timeout-not-nonzero-exit.md`, `drafts/key-facts-documents-phantom-files.md`. Code changes: `server.py` (`_claude()` now catches `CalledProcessError`/`FileNotFoundError` alongside `TimeoutExpired`, all three `ERROR:`-prefixed), `git_commit.py` (same two new `except` clauses), `scripts/check_key_facts.py` (new `project_files_table_rows()` + reverse phantom-file check), `docs/project_notes/key_facts.md` (removed `update_profile.py`/`template.md` rows — never committed to this repo).
  - https://dev.to/enjoy_kumawat/my-claude-p-wrapper-catches-timeouts-a-non-zero-exit-isnt-a-timeout-so-it-just-crashes-860
  - https://dev.to/enjoy_kumawat/my-projects-memory-file-named-two-scripts-git-has-no-record-either-one-was-ever-committed-5736

### 2026-07-28 - DEV.to: drafted replies for 5 pending comments (4 replies + 1 skip)

- **Status**: Completed
- **Description**: `reply_comments.py pending` returned 5 comments across 4 articles. Drafted 4: mads_hansen on the `update_article` blind-write piece (confirmed the post-PUT log call still leaves a crash window, and that no conditional-write primitive exists yet), alexshev on the git-hook-install piece (confirmed the fix is silent — no version/timestamp receipt), alexshev on the comment-pipeline "handled" piece (confirmed already fixed — `needs_reply()` now checks latest speaker, not ever-replied), alexshev on the dedup piece (confirmed already fixed — `pending()` now matches `^## (\S+)` headers into a set instead of raw substring `in`). Skipped 1 (cailab, `3c102`) as spam — referenced "SRE Sidekick," a Vercel import issue, and OTel tracing conventions that don't exist anywhere in this repo; didn't engage the actual article.
- Source: `drafts/comment_replies.md`.

---

### 2026-07-27 - DEV.to: 1 article published (second run of the day) — quality bar, not forcing more

- **Status**: Completed (live; verified via HTTP 200 fetch of the URL)
- **Description**: Live quota check showed 2 already published today (first run). Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments. Most top scorers were repeats of things already excluded by prior runs' extensive logs, or too close to this repo's own already-mined veins: "Pin your MCP server contracts..." and "Tool Schema Drift..." both too close to the 2026-07-23 unpinned-contract post; "I gave my AI agent the ability to send email" / "I Connected 3 MCP Servers to One Agent" already excluded (single-MCP-server repo, no genuine angle); a DCO/code-signing angle on the vercel/eve #454 PR was considered against the "AI And Code Ownership" trend but dropped as too close to the 2026-07-15 "kept signing its own work" post. One genuine distinct angle survived: "I Let an Agent Take Over an Account With Every Permission Check Green" (agents/security, r9 c15) — the same "check passed, outcome still bad" shape, but found live in this repo's own code rather than forced onto it. Audited `server.py`'s MCP tools for the same shape and found `update_article(article_id: int, ...)`: takes a bare integer, PUTs whatever fields are given straight to dev.to's API, with no fetch-before-write, no diff, and no log — a wrong or hallucinated article_id would silently overwrite a live published post with zero trace. Distinct from prior MCP-security posts (both were about vetting *other* servers before install / prompt-injection risk from third-party tools; this is a blast-radius gap in a tool in my *own* server). Fixed: `update_article` now fetches the article's current state before writing, logs every write (article_id, changed fields, before/after title + published state) to `logs/article_updates.jsonl` via a new `_log_article_update()` helper, and returns a `diff` field in its response. Verified the diff/logging logic with an offline unit test against fake before/after states (deliberately did not test by mutating a real live article) — JSONL entry and diff both carried correct before/after pairs. `create_article` doesn't share this shape (a bad call there just makes an extra draft, cheap to notice) — `update_article` was the one tool where a plausible call and a wrong call took the identical code path.
  - Tags: mcp, ai, security, python. Source: `drafts/update-article-blind-write.md`. Code changes: `server.py` (`update_article` now does fetch-before-write + diff + audit log via new `_log_article_update()`/`_ARTICLE_UPDATE_LOG`).
  - https://dev.to/enjoy_kumawat/my-mcp-tool-can-overwrite-any-of-my-live-articles-with-just-an-integer-no-diff-no-log-no-warning-1caf

### 2026-07-27 - DEV.to: 2 articles published (first run of the day)

- **Status**: Completed (both live; verified via HTTP 200 fetch of each URL)
- **Description**: Session started with HEAD detached from `main` again (same recurring provisioning symptom, `bugs.md` 2026-07-18), local `main` 7 commits behind `origin/main` — recovered with `scripts/sync-main.sh` before any git-writing work, fast-forwarded cleanly, no divergence. Quota check via live API showed 0 published today (new UTC day; last entry was 2026-07-26 second run), so this was the first article-publishing run of the day; published 2 per the normal 1-3 range. Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments; every top scorer was already excluded by prior runs' extensive logs ("The Dirty Secret Behind AI Agents (Demo)", "AI And Code Ownership", "Choose your Burden", "I gave my AI agent the ability to send email", "'Local' Solves Where Your Data Goes...", "I Connected 3 MCP Servers to One Agent" — no genuine multi-server angle, this repo runs one MCP server) or too close to already-mined veins ("My AI pentest agent reported 23 root shells..." — faked-tool-result vein; "Log Every Agent Invocation... Stop Hook" — too close to the 2026-07-20 "zero log lines" post; "Tool Schema Drift: The Silent Failure Mode..." — too close to the 2026-07-23 unpinned-contract post). Instead of forcing a trend match, re-audited this exact repo's own code for gaps neither trend-matched nor previously written up, and found two, both verified live and fixed this run:
  1. `reply_comments.py`'s `replied_by_me()` (used by `pending()` to decide whether a comment thread still needs a reply) walked a comment's entire reply tree and returned `True` the moment it found *any* message from me, anywhere in the subtree — not whether the *latest* message was mine. Verified live with a repro against the real function: a thread where I replied on 2026-07-25 and the commenter posted a fresh follow-up on 2026-07-26 still returned `replied_by_me() == True`, meaning `pending()` would silently and permanently skip that thread — the follow-up would never resurface in any future run's output. Distinct from both prior fixes to this same function's neighborhood: the 2026-07-24 "36 drafted, 0 posted" post (drafted-vs-posted, a human-pasting-step gap) and the 2026-07-26 "substring collision" fix (a wrong-identity-match false positive in the dedup check). This is neither an identity bug nor a posting-step bug — it's a recency bug: the check could not distinguish "I have replied to this thread" from "I have seen the latest message in this thread," and those diverge the instant the other side speaks again. Fixed: replaced `replied_by_me()` with `latest_message()` (walks the subtree by `created_at`, DEV.to's comment API includes an ISO-8601 timestamp on every node) + `needs_reply()` (true iff the newest message's author isn't me). Rewrote `--selftest` with three cases (answered-and-quiet, answered-then-followed-up, untouched) — all pass. Reran `pending()` against the live API afterward: no crash, no output change for today's actual comment state (today's real inbox doesn't happen to be sitting on this exact case, but the repro against synthetic data confirms the bug was live).
  2. Re-verified the `hooks/prepare-commit-msg` install fix from `bugs.md` 2026-07-26 (which added `scripts/install-hooks.sh` and confirmed it working in that session's clone) by checking `.git/hooks/` in *this* session's fresh container before doing anything else: `ls .git/hooks/` showed only git's stock `.sample` files — `prepare-commit-msg` was not installed, less than 24 hours after the prior run verified it was. Root cause: `.git/hooks/` is untracked and this repo's sessions are provisioned by checking out a pinned commit SHA into a fresh container each time (same mechanism as the recurring detached-HEAD bug, `bugs.md` 2026-07-18) — running `install-hooks.sh` once, in one container, has no effect on the next container's `.git/hooks/`, which starts empty again regardless of how many times the fix has already "worked." The 2026-07-26 fix's own prevention note ("run `scripts/install-hooks.sh` once per fresh clone/container") was already being silently skipped on the very next opportunity to test it, because nothing in the repo's actual workflow re-runs it — the note was advice to a future run that never reads bug logs before acting. Confirmed `install-hooks.sh` itself is correct and idempotent (reran it standalone, clean byte-for-byte copy, no error) — the gap is entirely that nothing calls it a second time. Distinct from the 2026-07-26 post itself (found the hook was never installed, anywhere, ever) and the 2026-07-22 "stale tracking ref" post (same pinned-SHA-checkout root cause, different artifact — a git ref, not a hooks directory) — this is about the *fix* for the first bug inheriting the same "doesn't survive a fresh container" property as the bug it fixed, caught only by literally checking the file the prior post's own prevention note pointed at. Fixed: moved the `install-hooks.sh` call into the top of `scripts/sync-main.sh`, which is already documented as safe to run unconditionally at the start of every git-writing session and already runs first, every run — so the hook install is now a side effect of something the workflow already does unconditionally, not a separate manual step. Verified: reran `sync-main.sh`, hook reappears and matches source byte-for-byte.
  - Tags: python/debugging/agents/devtools; git/python/debugging/devtools. Sources: `drafts/comment-pipeline-checks-ever-not-latest.md`, `drafts/hooks-install-fix-didnt-survive-next-container.md`. Code changes: `reply_comments.py` (`replied_by_me()` replaced with `latest_message()`/`needs_reply()`, `pending()` updated, `--selftest` rewritten), `scripts/sync-main.sh` (now installs hooks at the top of every run).
  - https://dev.to/enjoy_kumawat/my-comment-pipeline-marks-a-thread-handled-the-moment-i-reply-once-a-follow-up-question-proved-313k
  - https://dev.to/enjoy_kumawat/my-fix-for-the-git-hook-never-installs-landed-yesterday-todays-container-didnt-have-it-either-4iie

### 2026-07-27 - DEV.to: 7 new comment replies drafted
- **Status**: Completed
- **Description**: Ran `reply_comments.py pending` — 7 pending comments across 6 articles ("I Gave My MCP Tool an ERROR: Convention...", "My AI-Attribution Filter Strips Any Commit Mentioning \"llm\"...", "My prepare-commit-msg Hook Got a Timeout Fix 3 Days Ago...", "My Project Has a Memory File...", "My GitHub Token Is Valid...403", "My MCP Server Has 8 Tools and Zero Log Lines..."), from `alexshev`, `mads_hansen_27b33ebfee4c9`, and `neelagiri65`. Checked each claim against current code before replying: confirmed `server.py`'s `_claude()` still has only two failure paths and no fixture-style test suite (alexshev's point on 3c00h stands); confirmed every MCP tool still returns a bare `str`/`dict` with no typed error variant, just a consistent `ERROR:` prefix (mads_hansen's structured-shape point on 3c0a8 stands); confirmed `_STRIP_RE` is already anchored-phrase, not bare-substring, so responded on why hard exclusion still fits this specific filter (3c00b); confirmed `hooks/prepare-commit-msg` has no self-reporting (no last-run timestamp/exit-state log) despite now being actually installed (3c00j); confirmed `scripts/check_key_facts.py` exists but is a manual check, not one that runs before an agent trusts `key_facts.md` (3c00a); confirmed via `bugs.md` 2026-07-22 that the GitHub 403 was proxy-authored, not a client-side rejection (3bpji); confirmed `server.py` still has zero logging calls (3bpjj). Drafted all 7 in `drafts/comment_replies.md`.

### 2026-07-26 - DEV.to: 2 articles published (second run of the day)

- **Status**: Completed (both live; verified via HTTP 200 fetch of each URL)
- **Description**: Session started with HEAD detached from `main` again (same recurring provisioning symptom, `bugs.md` 2026-07-18), local copy 5 commits behind `origin/main` — stashed in-progress fixes first (`git stash push -u`), ran `scripts/sync-main.sh` to fast-forward cleanly, then popped the stash back with no conflicts. Quota check via live API showed 2 published today already (this morning's first run), well under the 5-article cap and not at 0, so the "publish at least 2" floor didn't apply. Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments; top scorers were either already excluded by prior runs' extensive logs ("The Dirty Secret Behind AI Agents (Demo)", "AI And Code Ownership", "Choose your Burden", "I built an AI dev harness that isn't allowed to trust itself" — all repeatedly flagged off-voice or too close to the self-distrust/verification vein already mined) or only tangentially relevant ("I Connected 3 MCP Servers to One Agent. It Got Scary Fast." — this repo only runs a single MCP server, no genuine multi-server angle to ground it in). Instead of forcing a trend match against a well this repo's own logs have repeatedly flagged as close to exhausted after 40+ posts, re-audited this exact repo's current code for gaps neither trend-matched nor previously written up, and found two, both verified live and fixed this run:
  1. `reply_comments.py`'s `pending()` used `if c["id_code"] in drafted` as its "already drafted" dedup check, where `drafted` is the *entire raw text* of `drafts/comment_replies.md` — a substring check against the whole file, not against actual header id_codes. Its neighbor function `audit()` (added 2026-07-25) does this correctly, extracting `set(re.findall(r"^## (\S+)", drafted_text, re.M))`. Verified live against the real draft file: a hypothetical new, never-drafted id_code `"2026"` (a plausible 4-char lowercase-alphanumeric shape, same as real id_codes like `3b908`) is NOT in the set of real headers, but IS found as a raw substring of the file's prose (an unrelated sentence mentioning the year). `pending()`'s old check would have silently skipped this comment forever — no error, no log line, nothing — while `audit()` (which only tracks id_codes actually extracted as headers) has no visibility into a comment that was never even detected as pending in the first place. Distinct from the 2026-07-24 "36 drafted, 0 posted" post (drafted-vs-posted) and the 2026-07-25 memory-files post (a docs table, not this pipeline's own dedup logic) — this is a live substring-collision risk in the pipeline's core dedup, caught by noticing `pending()` and `audit()` do the same lookup two different ways, forty lines apart. Fixed: `pending()` now extracts `drafted_codes` via the same anchored header regex `audit()` uses, checked with `in drafted_codes` instead of `in drafted`. Reran `--selftest` clean; reran the collision simulation against the fix — confirmed `"2026" in drafted_codes` is `False` where the old raw-text check was `True`.
  2. Re-audited `server.py`'s `_claude()` helper (used by the `generate_commit_message` MCP tool) after the 2026-07-24 empty-diff-guard fix, which introduced an `"ERROR: ..."` prefix convention so callers can do `result.startswith("ERROR:")` to detect a failure instead of trusting the string as a real commit message. That convention was never checked against `_claude()`'s other, older failure path: on `subprocess.TimeoutExpired` (added 2026-07-24 in a separate pass, fixing the "no timeout" bug from 2026-07-23/2026-07-24), it returned the bare string `"claude -p timed out after 20s"` — no `ERROR:` prefix. A caller that adopted the empty-diff fix's own advice (check the prefix) would get `False` on a timeout and commit the literal explanatory sentence as a Conventional Commit subject. Confirmed by direct code inspection (regex-extracted both return statements) rather than by triggering a real 20s hang. Distinct from both prior fixes that touched this exact function on the same days (2026-07-23 timeout-added, 2026-07-24 empty-diff-guard-added) — this is about the two fixes never being reconciled against each other's signaling convention. Fixed: changed the timeout branch to return `"ERROR: claude -p timed out after 20s"`, matching the empty-diff branch. Verified via `ast.parse` and a direct regex check that both `generate_commit_message` failure paths now share the same prefix.
  - Tags: python, mcp, debugging, devtools; mcp, python, debugging, ai. Sources: `drafts/pending-comments-substring-collision.md`, `drafts/timeout-branch-missing-error-prefix.md`. Code changes: `reply_comments.py` (`pending()` now uses anchored header-regex extraction, matching `audit()`), `server.py` (`_claude()` timeout branch now returns an `ERROR:`-prefixed string).
  - https://dev.to/enjoy_kumawat/my-comment-dedup-check-used-in-on-a-whole-markdown-file-a-date-in-a-sentence-broke-it-gmf
  - https://dev.to/enjoy_kumawat/i-gave-my-mcp-tool-an-error-convention-i-only-taught-it-to-one-of-its-two-failure-paths-4619

### 2026-07-26 - DEV.to: 2 articles published (first run of the day)

- **Status**: Completed (both live; verified via HTTP 200 fetch of each URL)
- **Description**: Session started with HEAD detached from `main` again (same recurring provisioning symptom, `bugs.md` 2026-07-18) — recovered with `scripts/sync-main.sh` before any git-writing work, fast-forwarded 4 commits cleanly onto `origin/main`, no divergence. Quota check via live API showed 0 published today (new UTC day; last entry was 2026-07-25), so this was the first run of the day; published 2 per the normal 1-3 range. Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments; nearly every top scorer was already excluded by prior runs' extensive exclusion lists (git/agents/verification/memory/environment-provisioning veins all repeatedly flagged as mined out — see the last 5 days of entries above). Instead of forcing a trend match against a well this repo's own logs have flagged as close to exhausted (2026-07-23, repeated since), re-audited this exact repo's own tooling for gaps neither trend-matched nor previously written up, and found two, both verified live and both actually fixed this run (not just audited):
  1. Re-verified the `hooks/prepare-commit-msg` timeout fix from `bugs.md` 2026-07-23 by checking whether the hook was actually reachable by git in this environment, not just correct on disk: `git config --get core.hooksPath` and `git config --global --get core.hooksPath` both returned empty, and `ls .git/hooks/` showed only git's stock `prepare-commit-msg.sample` — no real `prepare-commit-msg`. The hook has never executed once in this environment since the repo's first commit. Root cause: `hooks/` is a plain tracked directory with no special meaning to git; only `.git/hooks/` (or `core.hooksPath`) is ever executed, and neither is part of the tracked repo — README's own install instructions (`git config --global core.hooksPath d:/codes/my_git_manger/hooks`, a Windows-absolute path, or a manual `cp hooks/prepare-commit-msg .git/hooks/`) are both one-time, machine/clone-local steps that don't survive a fresh `git clone` or this repo's pinned-SHA container checkout. Fixed: added `scripts/install-hooks.sh` (copies `hooks/*` into `.git/hooks/`, chmod +x), ran it for real against this clone, verified installed content matches source byte-for-byte, and verified the hook's own `[ "$2" = "" ] || exit 0` guard behaves correctly both when a message source is given (no-op, file untouched) and when none is given (attempts to run `git_commit.py`, which correctly detected a clean working tree and left the file empty). Distinct from the 2026-07-23 post (fixed the hook's internal timeout, assumed it ran) and the 2026-07-24 comment-reply-pipeline post (a manual-paste gap, not a git-hooks-path wiring gap) — same "the fix on paper isn't the fix in practice" shape, different mechanism.
  2. Re-audited the `_STRIP` bare-substring filter flagged in the 2026-07-22 article ("kept signing itself" mirror bug: `"llm"`/`"claude"`/`"anthropic"` as bare substrings erase any commit message that legitimately mentions this project's own AI-calling code) and confirmed, by rerunning the exact same test cases, that it was still unfixed in both `server.py` and `git_commit.py` four days later — that audit had proposed narrower patterns but explicitly stopped short of shipping them. Shipped the fix this run: replaced the bare-substring tuple with a word-boundary/phrase-anchored regex (`co-authored-by\s*:`, `generated (with|by)\s+claude`, `\bclaude code\b`, `\bwritten by (an )?(ai|llm|claude|chatgpt|copilot)\b`, `\bai-generated\b`, `🤖`) in both files. Verified against two sets before shipping: legitimate technical lines (`"fix: retry llm calls on 429 with backoff"`, `"docs: explain the claude cli fallback path"`, etc.) now survive unchanged; real attribution lines (`"Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>"`, `"🤖 Generated with Claude Code"`) are still stripped to empty. Confirmed both files compile cleanly post-edit. Distinct from the 2026-07-22 post itself (that was the audit-only finding) — this is the follow-through fix, and a note-to-self on treating "proposed a fix, didn't ship it" as a live TODO rather than a closed finding.
  - Tags: git/python/debugging/devtools; python/ai/debugging/devtools. Sources: `drafts/prepare-commit-msg-hook-never-ran.md`, `drafts/strip-filter-deletes-llm-commits-fixed.md`. Code changes: `scripts/install-hooks.sh` (new; hook installed for real in this clone's `.git/hooks/`), `server.py` (`_STRIP` → `_STRIP_RE` regex), `git_commit.py` (same).
  - https://dev.to/enjoy_kumawat/my-prepare-commit-msg-hook-got-a-timeout-fix-3-days-ago-it-has-never-once-executed-17o3
  - https://dev.to/enjoy_kumawat/my-ai-attribution-filter-strips-any-commit-mentioning-llm-including-the-ones-about-my-own-llm-65o

### 2026-07-25 - DEV.to: 1 article published (second run of the day) — quality bar, not forcing more

- **Status**: Completed (live; verified via HTTP 200 fetch of the URL)
- **Description**: Session started in detached HEAD again (same recurring provisioning symptom, `bugs.md` 2026-07-18) — recovered with `scripts/sync-main.sh` before any git-writing work, fast-forwarded 3 commits cleanly onto `origin/main`, no divergence. Quota check via live API showed 1 published today already (this morning's first run), well under the 5-article cap and not at 0, so the "publish at least 2" floor didn't apply. Note: the quota-check call itself hit a live `403 Forbidden Bots` on the first attempt because the ad-hoc script omitted a `User-Agent` header — that failure is what this run's article is actually about. Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments. Nearly every top scorer was already excluded by prior runs' log entries or off-voice: "The Dirty Secret Behind AI Agents (Demo)" (marketing-tone demo, off-voice, excluded repeatedly), "AI And Code Ownership" (used repeatedly since 2026-07-21), "Choose your Burden" (off-voice community-discuss piece), "The Person Who Fixed the Bugs Just Vanished" (no clean first-hand hook), "I built an AI dev harness that isn't allowed to trust itself" (repeatedly too close to the self-distrust/verification vein), "Stratagems #18" (off-voice fiction), "I Built an AI Memory Agent That Forgets on Purpose" (too close to the published cross-session-memory/FTS5 vein without a fresh angle), "4 Silent Failures, 2 Undocumented APIs, and a Container That Crashed" (repeatedly too close to the environment-provisioning vein), "How I reduced AI coding context by 95%" (too close to the published context-flooding/context-mode vein), "I gave my AI agent the ability to send email" and "Pin your MCP server contracts" (both used 2026-07-23). Instead of forcing a trend match, followed up on the live 403 hit while checking quota: `publish_devto.py` and `reply_comments.py` both carry the comment "dev.to 403s the default urllib UA," fixed weeks ago by spoofing `Mozilla/5.0` — but that comment was never actually tested against the real boundary. Ran a UA matrix live against `GET /api/articles/me`: confirmed the real rule is narrower and different in kind than documented — dev.to 403s any User-Agent containing the substring "urllib" (case-insensitive) or a missing/empty UA, not "anything that looks like a script." `python/1.0`, `foo-python-bar`, `curl/8.0`, and `PostmanRuntime/7.0` all pass clean; only strings containing "urllib" (including a made-up `foo-urllib-bar`) get blocked. This mattered concretely for `server.py`'s `_dev()` helper, which uses a third, undocumented UA string (`developer-presence-mcp/1.0`) that has silently worked since day one purely because it doesn't contain "urllib" — nobody chose it for that property, and no comment explained it. A well-intentioned rename to something more descriptive (e.g. containing "urllib-client") would have silently 403'd all four DEV.to MCP tools with an empty response body and no diagnosable error shape. Distinct from the 2026-07-22 "GitHub proxy-shaped 403" post (a sandbox-proxy artifact on a different platform/mechanism) — this is a real Forem/dev.to bot-detection substring rule on the live API, verified with a UA test matrix, not a proxy artifact. Fixed this run: corrected the misleading comments in both scripts to describe the tested substring rule, and added a documented `_DEV_UA` constant + assert guard in `server.py` so a future rename can't reintroduce the bug silently.
- **Quality-bar call**: Published 1 instead of forcing 2-3, per the task's own quality bar — today's second consecutive run where the trending sweep produced no second strong, genuinely distinct angle without drifting into already-mined veins. Repeating the flag from this morning's and 2026-07-23's entries: this repo's material is close to exhausted after 40+ posts against a small codebase; still worth a user decision on cadence, source-material scope, or a review gate.
- Tags: python/mcp/debugging/devtools. Source: `drafts/devto-user-agent-urllib-substring-block.md`. Code changes: `publish_devto.py`, `reply_comments.py` (corrected UA comment), `server.py` (`_DEV_UA` constant + assert guard on `_dev()`).
- https://dev.to/enjoy_kumawat/my-scripts-say-devto-blocks-the-default-urllib-user-agent-thats-not-the-actual-rule-3pi5

### 2026-07-25 - DEV.to: 1 article published (first run of the day) — quality bar, not forcing more

- **Status**: Completed (live; verified via HTTP 200 fetch of the URL)
- **Description**: Session started in detached HEAD again (same recurring provisioning symptom, `bugs.md` 2026-07-18) — recovered with `scripts/sync-main.sh` before any git-writing work, fast-forwarded 2 commits cleanly onto `origin/main`, no divergence. Quota check via live API showed 0 published today (new UTC day, no prior publish-run entry today — only a comment-reply-drafting entry), so this was the first article-publishing run of the day. Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments. Nearly every top scorer was already excluded by prior runs' log entries: "AI And Code Ownership" (used repeatedly as a source trend since 2026-07-21, most recently reframed for the stop-hook/history-rewrite post), "The Dirty Secret Behind AI Agents (Demo)" (marketing-tone demo, off-voice), "Choose your Burden" (off-voice community-discuss piece), "The Person Who Fixed the Bugs Just Vanished" (bus-factor/maintainer-vanishing angle, no clean first-hand hook into this repo), "Loop Engineering: Stop Your Agent Reward-Hacking Its Own Checks" / "...Stop the Sycophancy" (both scored decently but any repo-grounded angle risked landing back in the self-referential "my own log audits its own claims" shape this repo has already published and been explicitly flagged for overusing, see 2026-07-23 entry). Instead, followed up on a genuinely unresolved thread from this repo's own history: a 2026-07-19 comment-reply drafted in response to a reader's periodic-distillation proposal noted that `bugs.md`/`decisions.md`/`key_facts.md` (curated) vs. raw `issues.md` (append-only) is already an informal "forgets on purpose" pattern, but that "staleness detection in the distilled files is still manual" — that observation was never turned into an article or a check. Verified live: wrote `scripts/check_key_facts.py` (diffs backticked filenames in `key_facts.md`'s Project Files table against actual `.py`/`.sh` files in the repo root, `scripts/`, and `hooks/`). First run flagged `scripts/sync-main.sh` (load-bearing since 2026-07-18, never added to the table) missing entirely. `publish_devto.py` — the script this exact scheduled task calls every run — passed the mechanical check but only because its filename appears once, as a parenthetical inside the row for a different, already-deleted script (`post_article.py`); it had no row of its own, so an agent skimming the table for "what publishes articles" would never find it. The checker itself then missed `hooks/prepare-commit-msg` on its first run — a real bug in the checker, not the docs: git hook filenames have no extension, and the initial `.py`/`.sh` suffix filter walked past it. Fixed all three: gave `publish_devto.py` and `scripts/sync-main.sh` their own rows in `key_facts.md`, added `hooks/prepare-commit-msg`, and widened the checker to track `hooks/` unconditionally instead of by suffix. Reran clean (`key_facts.md is in sync with repo scripts.`). Distinct from the 2026-07-18 "memory file cost drift" post (issues.md growing bigger under a read-every-run contract — a size/cost problem) and the 2026-07-14 "citation lied without lying" post (one specific stale fact, already fixed, in `key_facts.md`'s token-scopes line) — this is about the curated files' own reference table silently losing sync with the filesystem because no run's job was ever "verify the summary still matches reality," only "append to the raw log."
- **Quality-bar call**: This is the ~41st–42nd article from this same 8-file-plus-docs repo across 2+ weeks of twice-daily runs. Today's trending sweep again produced no second strong, genuinely distinct angle without drifting into the self-referential-meta shape already flagged twice (2026-07-21, 2026-07-23) as overused. Published 1 instead of forcing 2-3, per the task's own quality bar. **Repeating the 2026-07-23 flag for the user**: this repo's material is close to exhausted after 40+ posts against a small codebase; worth a decision on cadence, source-material scope, or a review gate rather than each run continuing to self-diagnose the same shortage.
- Tags: ai/agents/python/devtools. Source: `drafts/memory-files-missing-the-actual-script.md`. Code changes: `docs/project_notes/key_facts.md` (added `publish_devto.py`, `scripts/sync-main.sh`, `hooks/prepare-commit-msg`, `scripts/check_key_facts.py` rows), `scripts/check_key_facts.py` (new).
- https://dev.to/enjoy_kumawat/my-project-has-a-memory-file-so-my-agent-doesnt-reread-everything-it-never-learned-my-most-used-3hgj

### 2026-07-25 - DEV.to: 3 new comment replies drafted
- **Status**: Completed
- **Description**: Ran `reply_comments.py pending` — 3 pending comments across 3 articles ("My Commit-Message Script Has an Empty-Diff Guard...", "I Fixed a Timeout Bug Two Days Ago...", "My Commit Hook Calls an LLM on Every Commit..."), from `mads_hansen_27b33ebfee4c9`, `cailab`, and `skillselion`. Checked each claim against current code before replying: confirmed `generate_commit_message` in `server.py` still returns a bare `ERROR:`-prefixed string with no hash binding to the staged tree (mads_hansen's point stands); confirmed `_claude()` in `server.py` and the inline `claude -p` call in `git_commit.py` are still duplicated, not extracted to a shared module (cailab's point stands); confirmed `hooks/prepare-commit-msg` already captures output to a shell variable before writing `$1` (matches skillselion's mktemp-file intent) but the timeout is still Python-level only, no shell-level `timeout` wrapper around the whole script invocation. Drafted all 3 in `drafts/comment_replies.md`.

---

### 2026-07-24 - DEV.to: 1 article published (second run of the day) — quality bar, not forcing a second

- **Status**: Completed (live; verified via HTTP 200 fetch of the URL)
- **Description**: Quota check via live API showed 2 published today already (this morning's first run), well under the 5-article cap and not at 0, so the "publish at least 2" floor didn't apply. Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments. Every top scorer was already excluded by prior runs' log entries or off-voice: "AI And Code Ownership" (used 2026-07-21), "The Dirty Secret Behind AI Agents (Demo)" (marketing-tone demo post, off-voice), "I built an AI dev harness that isn't allowed to trust itself" / "'Local' Solves Where Your Data Goes..." (both repeatedly judged too close to the self-distrust/verification vein), "I built a readability test for my own compression format" / "I measured every millisecond of my real-time AI pipeline" (both used 2026-07-21 as sources), "I gave my AI agent the ability to send email" and "Pin your MCP server contracts" (both used 2026-07-23), "Choose your Burden" and "Stratagems" (off-voice), "My AI pentest agent reported 23 root shells..." (too close to the faked-tool-result vein), "I added MCP servers to Claude Code. Here's what they cost in tokens." (too close to the already-published tool-schema-bloat post). Remaining candidates ("Loop Engineering: Stop the Sycophancy," "Log Every Agent Invocation... Stop Hook") scored low and any repo-grounded angle on them risked landing in the same self-referential-log-auditing shape already flagged as overused. Instead of forcing a trend match or another "log audits its own claims" piece, went back to a genuine unresolved thread from this repo's own history: the 2026-07-21 comment-reply log entry ("On 'My AI Commit Script and My MCP Tool Run the Exact Same Code,' a commenter's point that `-> str` alone doesn't solve the empty-diff failure sent me back to `server.py` — confirmed `generate_commit_message` has no empty-diff guard at all") was filed as a comment-reply observation and never turned into an article or a fix. Verified live before writing: called `_claude("", system=SYSTEM)` directly and got back `"There are no staged or unstaged changes in the repository, so there's nothing to generate a commit message for."` — not a crash, not a fabricated Conventional Commit, but free-form prose typed as a valid `str` return with nothing marking it as a failure. Ran a second test with a diff for a nonexistent file to rule out `claude -p` quietly checking real git state instead of the passed string — it used the fake diff verbatim (`'feat: add wombat teleporter stub function'`), confirming the function trusts its input completely and the empty-diff response is a genuine gap, not tool-use side-reasoning. Distinct from the 2026-07-19 "same code, one is agentic" post (that found the STRIP filter is identical across both paths) — this is the one place the two paths diverge: `git_commit.py` has an explicit `if not diff.strip(): ... raise SystemExit(1)` guard; the MCP tool had none. Risk: a caller piping the tool's return value straight into a commit (the tool's entire purpose) would commit the literal explanatory sentence as the commit message, since nothing distinguishes it from a real one. Fixed this run: added `if not diff.strip(): return "ERROR: empty diff — nothing to generate a commit message from."` to `generate_commit_message` in `server.py`, mirroring the script's guard in a way a caller can check (`.startswith("ERROR:")`).
- Tags: mcp/python/debugging/devtools. Source: `drafts/mcp-tool-no-empty-diff-guard.md`. Code changes: `server.py` (`generate_commit_message` empty-diff guard).
- https://dev.to/enjoy_kumawat/my-commit-message-script-has-an-empty-diff-guard-my-mcp-tool-version-doesnt-and-it-doesnt-fail-5h0i

### 2026-07-24 - DEV.to: 2 articles published

- **Status**: Completed (both live; verified via HTTP 200 fetch of each URL)
- **Description**: Session started with HEAD detached from `main` again (same recurring provisioning symptom, `bugs.md` 2026-07-18), local `main` 19 commits behind `origin/main` — recovered with `scripts/sync-main.sh` before any git-writing work, fast-forwarded cleanly (pulled in yesterday's comment-reply pipeline and `sync-main.sh` fetch-first fix), no divergence. Quota check showed 0 published today (new UTC day; last entry was 2026-07-23), so this was the first article-publishing run of the day; published 2 per the normal 1-3 range. Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments. Explicitly excluded top scorers already mined or off-voice: "AI And Code Ownership" (used 2026-07-21 first run), "Instrumenting an AI-Powered GitHub Analyzer with OpenTelemetry and SigNoz" (used 2026-07-20 second run), "Stratagems #17/#18" (off-voice fiction), "I built an AI dev harness that isn't allowed to trust itself" (repeatedly judged too close to the self-distrust/verification vein), "'Local' Solves Where Your Data Goes..." (too close to the 2026-07-15 MCP-trust-boundary post), "Retrieval-Augmented Self-Recall" (too close to FTS5-not-vectors/cross-session-memory), "I built a readability test for my own compression format" (used 2026-07-21 second run as source), "I measured every millisecond of my real-time AI pipeline" (used 2026-07-21 first run as source), "I gave my AI agent the ability to send email" (used 2026-07-23 first run as source), "Choose your Burden" (off-voice mental-health/community discuss piece), "4 Silent Failures, 2 Undocumented APIs, and a Container That Crashed" (repeatedly judged too close to the environment-provisioning vein), and two posts that turned out to be this repo's own prior articles resurfacing in its own `claudecode`-tag trending feed ("I Almost Hand-Rolled JSON-RPC for an MCP Server," "My AI Commit Script and My MCP Tool Run the Exact Same Code"). No single trending post scored high with a genuinely fresh angle this run, so instead of forcing a match, re-audited this exact repo's own code for gaps neither trend-matched nor previously written up — found two, both verified live before writing:
  1. Re-grepped `subprocess.check_output(` and `urlopen(` across the repo after the 2026-07-23 "commit hook had no timeout" fix (which touched only `git_commit.py`/`hooks/prepare-commit-msg`) and found the identical bug still present, two days later, in `server.py`'s `_claude()` helper (used by the `generate_commit_message` MCP tool) — no `timeout=` on its `subprocess.check_output` call — plus `_gh()` and `_dev()`'s `urlopen()` calls, which also had no timeout, unlike `publish_devto.py`/`reply_comments.py` which both explicitly pass `timeout=30`. Fixed all three this run: `timeout=20` + graceful degrade (matching `git_commit.py`'s pattern, not a raise) on `_claude()`, `timeout=30` on both `_gh()`/`_dev()` `urlopen()` calls. Distinct from the 2026-07-19 "same code, one is agentic" post (that was about the two paths being provably identical) and the 2026-07-23 timeout post itself (that fix's scope was the file it was found in, not the pattern) — this is about a fix that didn't generalize to its own twin.
  2. Audited whether the comment-reply-drafting pipeline (built 2026-07-18, `bugs.md`/`decisions.md` same date) has actually resulted in any on-site replies since. Added an `audit` command to `reply_comments.py` that cross-checks every id_code in `drafts/comment_replies.md` against the live comment thread's `children` for a reply from `enjoy_kumawat` (reusing the same check `replied_by_me()` already does for the *pending* side). Result, verified live: 36 drafted, 0 posted — every single drafted reply since the pipeline's creation. Root cause: the "paste manually, then delete the entry" step assumes a human periodically opens the file; this pipeline runs fully unattended twice daily, so nobody ever does. The dedup logic (id_code-in-file) can't distinguish "replied" from "drafted," so the file's growth looked identical to the backlog shrinking from inside the system. Distinct from the 2026-07-19 "20 unanswered comments, nothing told me" post (that was about discovering the *original* backlog before this pipeline existed) — this is the pipeline built to fix that backlog quietly producing zero actual fixes for six days, caught only by pointing an existing check at the pipeline's own output instead of just other people's articles.
  - Tags: mcp/python/debugging/devtools; ai/python/devtools/productivity. Sources: `drafts/mcp-tool-copy-of-fixed-timeout-bug.md`, `drafts/comment-drafts-never-posted.md`. Code changes: `server.py` (`_claude`/`_gh`/`_dev` timeouts), `reply_comments.py` (`audit` command).
  - https://dev.to/enjoy_kumawat/i-fixed-a-timeout-bug-two-days-ago-the-copy-of-that-code-inside-my-mcp-server-still-has-it-13bo
  - https://dev.to/enjoy_kumawat/my-comment-reply-pipeline-has-drafted-36-replies-since-i-built-it-it-has-posted-zero-3b72

### 2026-07-24 - DEV.to: 6 new comment replies drafted
- **Status**: Completed
- **Description**: Ran `reply_comments.py pending` — 6 pending comments across 6 articles ("My Commit Hook Calls an LLM on Every Commit," "My MCP Tool Defaults to Draft Mode," "My GitHub Token Is Valid. My MCP Server Still Gets a 403," "Git Told Me I Was 14 Commits Ahead of Origin," "A Post-Commit Hook Told Me to Rewrite 8 Pushed Commits," "df Said My Sandbox Had No Disk Left"), all from `gnlassi`, `mads_hansen_27b33ebfee4c9`, `alexshev`, and `eduzsh`. Drafted all 6 in `drafts/comment_replies.md`, grounded in each article's log entry above rather than reused boilerplate. Notable ones: on "My MCP Tool Defaults to Draft Mode," a commenter's two-phase-authority/immutable-draft-hash proposal named the actual gap precisely — no promotion step with its own revocable credential exists today. On "My GitHub Token Is Valid...403," a commenter's trusted-header-at-the-boundary idea was compared against what `_gh`/`_dev` in `server.py` actually do (nothing — origin-vs-policy is only distinguishable by manually hitting `$HTTPS_PROXY/__agentproxy/status`).



### 2026-07-23 - DEV.to: 1 article published (second run of the day) — deliberately under quota, well of fresh material is thin

- **Status**: Completed (live; verified via HTTP 200 fetch of the URL). Quota check showed 2 published today already (this morning's run), well under the 5-article cap and not at 0, so the "publish at least 2" floor didn't apply.
- **Description**: Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments (full table in this run's tool output). Every top-scoring item was already excluded by prior runs' log entries or off-voice: "AI And Code Ownership" / "Every AI-Generated Line of Code Is a Small Loan" (used repeatedly since 2026-07-18/21), "Instrumenting an AI-Powered GitHub Analyzer with OpenTelemetry and SigNoz" (used 2026-07-20), "Stratagems" fiction series (off-voice, excluded every run it appears), "What is an 'agentic harness,' actually?" (used 2026-07-19), "I built an AI dev harness that isn't allowed to trust itself" / "'Local' Solves Where Your Data Goes..." / "Retrieval-Augmented Self-Recall" (all repeatedly judged too close to already-published verification/trust-boundary/memory posts), "Pin your MCP server contracts" (used this morning). Remaining on-voice candidates ("Log Every Agent Invocation... Stop Hook," "Claude might be saturating your machine," "Loop Engineering: Stop the Sycophancy") all scored low (≤45) — weak trending signal, not strong enough to anchor a piece on their own.
- **Explicit quality-bar call**: this is the ~32nd–33rd article from a very small 8-file repo (`server.py`, `git_commit.py`, `publish_devto.py`, `reply_comments.py`, `scripts/sync-main.sh`, `hooks/prepare-commit-msg`) across 10+ days of twice-daily runs. Nearly every file has already been mined multiple times (see `bugs.md`/`decisions.md` — most entries there already have a corresponding published article). Per the task's own quality bar ("publish fewer articles... rather than forcing weak ones"), published 1 instead of forcing 2–3.
- **What got published**: re-read `hooks/prepare-commit-msg` and `git_commit.py` closely (not reused from a prior write-up) and found a real, previously-undocumented bug: neither `subprocess.check_output` call in `git_commit.py` (the `git diff --staged` call or the `claude -p` call) had a `timeout`, so a hung `claude` CLI process would hang `git commit` indefinitely with no diagnostic. Separately, the hook's `python "$SCRIPT" > "$1" 2>/dev/null || true` truncates the commit-message file via shell redirect *before* Python produces any output, so any failure path (missing `claude` on PATH, a crash) wipes git's own pre-filled template to empty, silently, since stderr is discarded and `|| true` swallows the exit code — git's own empty-message-abort guardrail happened to prevent worse (empty-message commits), but that was luck, not design. Reproduced the hang deliberately (wrapper script that `sleep 300`s instead of the real `claude` binary; confirmed with `ps` that the hang was the subprocess, not git). Fixed both: added `timeout=20` + a `TimeoutExpired` handler to `git_commit.py`, and changed the hook to capture output into a shell variable first, only overwriting `$1` if the script exited zero with non-empty output — so every failure path now leaves git's default template alone instead of wiping it. Distinct from the 2026-07-19 "same code, agentic or not" post and the 2026-07-22 "commit message deletes any commit about LLMs" post — neither touched reliability/timeout/silent-truncation behavior.
- Tags: claudecode/python/debugging/devtools. Source: `drafts/commit-hook-llm-no-timeout.md`. Code changes: `git_commit.py`, `hooks/prepare-commit-msg`.
- https://dev.to/enjoy_kumawat/my-commit-hook-calls-an-llm-on-every-commit-it-had-no-timeout-so-neither-did-git-commit-bpj
- **Flag for the user**: this run's research step surfaced that the source repo's genuine material is close to exhausted after 10+ days of twice-daily publishing — most remaining trending-topic candidates score low and most repo files have multiple prior articles against them already. Recent days' entries (2026-07-20 through 2026-07-22) show a visible drift toward increasingly self-referential meta-content (articles about the publishing log itself, about whether its own "distinct" claims are checked, about the pipeline's own quota logic). Also still open from this morning's first-run entry below: the unattended publish path (`publish_devto.py`, driven by this scheduled task) has no dry-run/hold-for-review step or kill switch, unlike the MCP tool's `published: bool = False` default — flagged, not fixed. Worth a human decision on cadence, source-material scope, or whether to add a review gate, rather than this routine continuing to self-diagnose it run after run.

### 2026-07-23 - DEV.to: 2 articles published (first run of the day)
- **Status**: Completed (both live; verified via HTTP 200 fetch of each URL)
- **Description**: Session started with HEAD detached from `main` again (same recurring provisioning symptom, `bugs.md` 2026-07-18), local `main` 16 commits behind `origin/main` — recovered with `scripts/sync-main.sh` before any git-writing work, fast-forwarded cleanly, no divergence. Quota check showed 0 published today (new UTC day; last entry was 2026-07-22 second run), so this was the first run of the day; published 2 per the normal 1-3 range. Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments. Explicitly excluded top scorers already mined or off-voice: "Every AI-Generated Line of Code Is a Small Loan" (used 2026-07-18 as source), "AI And Code Ownership" (used 2026-07-21 first run), "Instrumenting an AI-Powered GitHub Analyzer with OpenTelemetry and SigNoz" (used 2026-07-20 second run), "Stratagems #18" (off-voice fiction, repeatedly excluded), "What is an 'agentic harness,' actually?" (used 2026-07-19 first run), "I built an AI dev harness that isn't allowed to trust itself" (already judged too close to the self-distrust/verification vein on 2026-07-22), "'Local' Solves Where Your Data Goes. It Doesn't Solve What Your Agent Does" (already judged too close to the 2026-07-15 MCP-trust-boundary post on 2026-07-22), "Retrieval-Augmented Self-Recall" (repeatedly judged too close to FTS5-not-vectors/cross-session-memory), "I built a readability test for my own compression format. It scored 0/24" (used 2026-07-21 second run as source), "I measured every millisecond of my real-time AI pipeline" (used 2026-07-21 first run as source), "Unused MCP Plugins Are Quietly Eating Your Context" (used 2026-07-21 second run as source), "4 Silent Failures, 2 Undocumented APIs, and a Container That Crashed" (already skipped 2026-07-22 as too adjacent to the environment-provisioning vein), "My Agent Shipped 3 PRs in an Evening. 40% Corrections" (already judged too close to the scoring-formula/detached-HEAD posts on 2026-07-21), "Choose your Burden" (off-voice mental-health/community discuss piece), and three posts that turned out to be this repo's own prior articles resurfacing in its own trending feed ("I Almost Hand-Rolled JSON-RPC for an MCP Server," "My Project's Own Instructions Told My Agent to Use Tools That Don't Exist," "My AI Commit Script and My MCP Tool Run the Exact Same Code"). Picked 2 themes with a distinct first-hand angle, both freshly mined by rereading this exact repo's own code rather than reusing a prior incident:
  1. "Pin your MCP server contracts the way you pin your dependencies" trend (mcp, score 29) → reframed around a genuinely new gap in this repo's own 2026-07-14 fix: pinning `requirements.txt`'s `mcp[cli]>=1.28.0,<2.0.0` closed the *library* version risk, but the actual tool contract an agent reads — parameter names/types and docstrings on `server.py`'s `@mcp.tool()`-decorated functions — is generated fresh from source on every boot with no snapshot test, no schema fixture, nothing that would catch a rename, a type change, or a docstring edit silently changing what a connected agent sees. Verified live: `grep -rn "list_tools\|inputSchema\|get_schema"` across the repo returns nothing. Distinct from the 2026-07-03 tool-schema-bloat post (upfront token cost of a large schema) and the 2026-07-18 FastMCP-decorators post (framework choice vs. hand-rolled JSON-RPC) — this is about the generated contract drifting silently between commits, never previously tested.
  2. "I gave my AI agent the ability to send email" trend (mcp, score 42) → reframed around a genuinely new gap found by comparing this repo's two DEV.to-publish code paths side by side: the MCP tool `create_article` defaults to `published: bool = False` (a real safety net for interactive sessions), but this exact scheduled routine never calls that tool — it shells out to `publish_devto.py`, whose `published` flag comes straight from a draft file's frontmatter, which this task's own Step 3 instructions set to `true` every run. Went in expecting to find the same "same code, different interface" story as the 2026-07-19 "my AI commit script and my MCP tool run the exact same code" post and confirmed the opposite: these two paths are *not* equivalent, and the difference is exactly the one thing that looked like a safety feature. Catalogued what guardrails the unattended path actually has (fresh daily-cap check against the live API, title dedup, a quality-bar floor) versus what it doesn't (no dry-run/hold-for-review step, no kill switch before the `POST` fires) — logged the gap, did not ship a fix this run.
  - Tags: mcp/python/devtools/agents; mcp/ai/security/python. Sources: `drafts/mcp-schema-drift-unpinned-contract.md`, `drafts/agent-with-publish-authority-two-paths.md`.
  - https://dev.to/enjoy_kumawat/my-requirementstxt-is-pinned-my-mcp-servers-actual-contract-isnt-and-nothing-would-catch-it-14ng
  - https://dev.to/enjoy_kumawat/my-mcp-tool-defaults-to-draft-mode-the-script-that-actually-publishes-my-blog-doesnt-call-it-4176

### 2026-07-22 - DEV.to: 2 articles published (second run of the day)
- **Status**: Completed (both live; verified via HTTP 200 fetch of each URL)
- **Description**: Session started with HEAD detached from `main` again (same recurring provisioning symptom, `bugs.md` 2026-07-18), local `main` 15 commits behind `origin/main` — recovered with `scripts/sync-main.sh` before any git-writing work, fast-forwarded cleanly, no divergence. Quota check showed 2 published today already (from this morning's first run), well under the 5-article cap and not at 0, so the "publish at least 2" floor didn't apply — published 2 more per the normal 1-3 range, landing today's total at 4. Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments. Explicitly excluded top scorers already mined or off-voice: "Every AI-Generated Line of Code Is a Small Loan" (used 2026-07-18), "AI And Code Ownership" (used 2026-07-21 first run), "Choose your Burden" (off-voice mental-health/community discuss piece despite high score), "Stratagems #15"/"#18" (off-voice fiction), "I built an AI dev harness that isn't allowed to trust itself" (too close to the self-distrust/verification vein), "Instrumenting an AI-Powered GitHub Analyzer with OpenTelemetry and SigNoz" (used 2026-07-20 second run), "Building an AI Agent That Knows When Not to Guess" (repeatedly used prior source trend), "What is an 'agentic harness,' actually?" (used 2026-07-19 first run), "Post-Mortem: Building a Local MCP Server...ChromaDB" (repeatedly judged too close to the FTS5-not-vectors post), "'Local' Solves Where Your Data Goes. It Doesn't Solve What Your Agent Does" (too close to the 2026-07-15 "MCP server only talks to APIs I trust" post — same data-vs-pipe-trust boundary), "Retrieval-Augmented Self-Recall" (too close to FTS5-not-vectors/cross-session-memory), "I built 8 security layers for an MCP marketplace" (used 2026-07-19 second run), "I built a readability test for my own compression format. It scored 0/24" (used 2026-07-21 second run as source), "Teaching a Qwen agent to forget" (already judged too close to stale-memory/tools-that-don't-exist posts). Skipped forcing a match on "4 Silent Failures, 2 Undocumented APIs, and a Container That Crashed" (too adjacent to the already heavily-mined environment-provisioning vein: detached HEAD, stale tracking ref, df-lied-about-disk). Instead mined two real, live-verified findings from this run's own tool code and network path, neither previously written up:
  1. Real incident surfaced while sanity-checking `server.py`'s `_gh()` helper against a live token in this cloud sandbox (distinct machine from the Windows box `key_facts.md` documents it running on): `GET /users/enjoykumawat` returned `403` with a GitHub-API-shaped JSON body (`message` + `documentation_url` keys), but `documentation_url` pointed at `docs.anthropic.com/en/docs/claude-code/github-actions` — never a real GitHub response. Confirmed via `$HTTPS_PROXY/__agentproxy/status` that this session's outbound HTTPS runs through a local agent proxy, and via the session's own declared GitHub scope (`enjoykumawat/my-git-manager` only, via the GitHub MCP server) that raw `api.github.com` calls bypassing that channel get intercepted and answered with a same-shaped synthetic 403 rather than a connection-level rejection — confirmed two distinct error bodies for user-scoped vs. repo-scoped paths, both proxy-authored. Distinct from all four prior MCP-security posts (build-side hardening, install-side vetting, tool-schema bloat, untrusted-payload-through-a-trusted-pipe) and from the "dev.to API write wall is a feature" post (that was a genuine origin-server boundary; this is a local sandbox artifact that mimics the origin server's error shape closely enough to defeat naive error handling) — never previously tested because `_gh()` has only ever been exercised against the real Windows-side token.
  2. Real, live-tested gap found by re-auditing the `_STRIP` filter from the 2026-07-15 "kept signing its own work" fix: simulated `apply_strip()` against realistic commit subjects this exact repo's own `generate_commit_message` tool could plausibly produce ("fix: retry llm calls on 429 with backoff", "feat: add llm-based tag scoring for trending posts", etc.) — every one came back as an empty string, because the `"llm"` and `"ai:"` entries in `_STRIP` are bare substrings with no word-boundary check, and this repo's own code is unusually likely to generate commit subjects that legitimately mention "llm" (the feature the tool implements). Distinct from the original 2026-07-15 post (that was under-filtering: an instruction that didn't reliably stop AI attribution) — this is the mirror bug in the same tuple: over-filtering, a mechanical fix precise enough to catch attribution but blunt enough to erase legitimate content sharing a substring with the target phrase. Never caught before because no real commit in this repo's month of history has needed the word "llm" in its subject line — an unexercised branch, same shape as the sync-main.sh guard-clause finding from this morning's run. Proposed narrower replacement patterns (`"as an ai"`, `"llm-generated"`) but did not ship the patch — today's finding is the audit, not yet the fix.
  - Tags: mcp/security/debugging/devtools; ai/python/git/devtools. Sources: `drafts/github-proxy-shaped-403.md`, `drafts/strip-filter-deletes-llm-commits.md`.
  - https://dev.to/enjoy_kumawat/my-github-token-is-valid-my-mcp-server-still-gets-a-403-and-github-never-saw-the-request-4e0m
  - https://dev.to/enjoy_kumawat/i-fixed-my-commit-message-from-signing-itself-the-fix-also-deletes-any-commit-about-llms-3ol6

### 2026-07-22 - DEV.to: 2 articles published (first run of the day)
- **Status**: Completed (both live; verified via HTTP 200 fetch of each URL)
- **Description**: Session started with HEAD detached from `main` again (same recurring provisioning symptom, `bugs.md` 2026-07-18), 14 commits ahead of what local `refs/remotes/origin/main` showed — recovered with `scripts/sync-main.sh` before any git-writing work. Quota check showed 0 published today (new UTC day; last entry was 2026-07-21), so this was the first run of the day; published 2 per the normal 1-3 range. Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments. Explicitly excluded top scorers already mined or off-voice: "Every AI-Generated Line of Code Is a Small Loan" (used 2026-07-18 as source trend), "AI And Code Ownership" (used 2026-07-21 first run as source), "Stratagems #15"/"Stratagems #18" (off-voice fiction), "Instrumenting an AI-Powered GitHub Analyzer with OpenTelemetry and SigNoz" (used 2026-07-20 second run), "Building an AI Agent That Knows When Not to Guess" (repeatedly used prior source trend), "What is an 'agentic harness,' actually?" (used 2026-07-19 first run), "Post-Mortem: Building a Local MCP Server...ChromaDB" (repeatedly judged too close to the FTS5-not-vectors post), "I built an AI dev harness that isn't allowed to trust itself" (already judged too close to the self-distrust/verification vein on 2026-07-21), "I built 8 security layers for an MCP marketplace" (used 2026-07-19 second run), "Retrieval-Augmented Self-Recall" (already judged too close to FTS5-not-vectors/cross-session-memory on 2026-07-21), "Unused MCP Plugins Are Quietly Eating Your Context" (used 2026-07-21 second run as source), "My Agent Shipped 3 PRs in an Evening. 40% Corrections" (already judged too close to the scoring-formula/detached-HEAD posts on 2026-07-21). Instead of forcing a trend match, mined two real incidents surfaced by this run's own git recovery step itself — neither previously written up:
  1. Real incident from auditing `scripts/sync-main.sh` (the recurring detached-HEAD fix, written 2026-07-18): grepped every log entry since for its failure-branch message ("needs manual review") and found zero matches across ~12 runs — the `else` branch has never once fired, because this pipeline's structural conditions (single sequential session, nobody rewriting `main`) mean the detached commit is always a clean descendant. Verified the branch actually works by simulating a genuine divergence in a throwaway scratch repo (checked out an earlier commit, committed a sibling, ran the script against it) — confirmed correct exit-1 + message, but noted the downstream reaction (an agent reading stderr and halting rather than pushing forward) had never been exercised either. Distinct from the 2026-07-18 root-cause post (why HEAD detaches) and the "wrong diagnosis" post (re-testing a claim against recurrence) — this is about a guard clause for "this shouldn't happen" that's been code-reviewed but never actually run.
  2. Real incident from this run's own recovery: right after `sync-main.sh` fast-forwarded `main`, `git status` reported "ahead of origin/main by 14 commits" — but `git push` reported "Everything up-to-date," proving no real divergence existed. Root-caused via `git reflog show refs/remotes/origin/main`: only two entries, the clone-time shallow fetch (which had set the tracking ref to a commit 14 behind the actual pinned detached-HEAD SHA) and the just-run push. Concluded the pinned-SHA checkout step that produces the detached HEAD fetches/checks out that specific commit object directly, which doesn't touch `refs/remotes/origin/main` the way a named-branch fetch would — leaving the tracking ref stale relative to HEAD from the moment the container boots, before any commands run. Fixed for real: added `git fetch origin main -q 2>/dev/null || true` to the top of `sync-main.sh` so any later "ahead of"/"behind" comparison is against fresh data. Distinct from the "wrong diagnosis" post (that covered why HEAD itself detaches) — this is about a different ref (the local tracking cache of the remote) going stale for a related but separate reason, previously only a one-line aside in the 2026-07-18 log, never root-caused or fixed until now.
  - Tags: git/ai/devtools/debugging; git/debugging/ai/devtools. Sources: `drafts/sync-main-untested-failure-branch.md`, `drafts/stale-origin-tracking-ref.md`.
  - https://dev.to/enjoy_kumawat/i-wrote-a-recovery-script-for-a-bug-that-recurs-every-run-its-failure-branch-had-never-once-fired-3pkh
  - https://dev.to/enjoy_kumawat/git-told-me-i-was-14-commits-ahead-of-origin-i-wasnt-my-local-copy-of-originmain-was-just-329h

### 2026-07-21 - DEV.to: 2 articles published (second run of the day)
- **Status**: Completed (both live; verified via HTTP 200 fetch of each URL)
- **Description**: Session started with HEAD detached from `main` again (same recurring provisioning symptom, `bugs.md` 2026-07-18) — recovered with `scripts/sync-main.sh` before any git-writing work; local `main` fast-forwarded 13 commits onto `origin/main` cleanly, no divergence. Quota check showed 2 published today already (from this morning's first run), well under the 5-article cap and not at 0, so the "publish at least 2" floor didn't apply — published 2 more per the normal 1-3 range, landing today's total at 4. Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments. Explicitly excluded top scorers already mined or off-voice: "8 Things Developers Confidently Explain After Watching One YouTube Video" (off-voice joke listicle, top score), "Every AI-Generated Line of Code Is a Small Loan" (used 2026-07-18 as source), "Stratagems #15"/"Stratagems #18" (off-voice fiction), "AI And Code Ownership" (used this morning's own source trend), "Instrumenting an AI-Powered GitHub Analyzer with OpenTelemetry and SigNoz" (used 2026-07-20 second run), "Building an AI Agent That Knows When Not to Guess" (repeatedly used prior source trend), "How I made a Rust hot path 27x faster..." (used 2026-07-19 second run), "What is an 'agentic harness,' actually?" (used 2026-07-19 first run), "Six experiments on adversarial verification" (repeatedly used prior source trend), "I built an AI dev harness that isn't allowed to trust itself" (too close to the already heavily-mined self-distrust/verification vein: second-AI-reviewer, tests-pass, faked-tool-result, adversarial-verification-wall posts), "Post-Mortem: Building a Local MCP Server...ChromaDB" (repeatedly judged too close to the FTS5-not-vectors post), "I built 8 security layers for an MCP marketplace" (used 2026-07-19 second run), "A Dash of dev.to: My Blog Stats Now Live in the Terminal" (used 2026-07-20 second run as source), "AI Agent Cost Drift" (used 2026-07-18 as source), "Retrieval-Augmented Self-Recall" (too close to the FTS5-not-vectors and cross-session-memory posts), "AI Agent Shipped 3 PRs...40% Corrections" and "I Built a Task Orchestrator, Then Deleted Its Best Number" (both too close to the already-published "scoring formula never scores itself" and detached-HEAD-recurrence posts — same self-correction-rate vein, no fresh angle left in it). Picked 2 themes with a distinct first-hand angle:
  1. "I built a readability test for my own compression format. It scored 0/24" trend (llm, score 38) → reframed around a genuinely new gap: every log entry in this exact file writes a paragraph arguing the new post is "distinct" from ~30 prior ones, but that claim is entirely self-graded — nobody had ever checked it against the one external signal available, DEV.to's own engagement API (the same `reactions + 3×comments` formula this task already applies to other people's trending posts every run, per the 2026-07-20 second-run post, but never to pairs of my own posts that explicitly claimed to differ from each other). Pulled real numbers for the two "detached HEAD" posts (2026-07-15/2026-07-18, both claiming distinctness) and five MCP-security posts spanning three weeks — all near-zero, no signal either way. Distinct from the "scoring formula never scores itself" post (that was a number never computed at all) — this is about two different claims ("editorially distinct" vs. "readers can tell") being treated as one, with only the first ever actually checked.
  2. Reinforced by the mcp-tag "Unused MCP Plugins Are Quietly Eating Your Context: A Weekly Auto-Disable Pipeline" trend (score 10) → reframed around a real, previously-unwritten wrinkle in `decisions.md` ADR-005: the blanket `drafts/*` gitignore rule (fixed 2026-07-18) needed one specific negated exception, `!drafts/comment_replies.md`, because that one file is a live dedup ledger `reply_comments.py` reads/writes across runs, not a disposable draft, despite matching the exact same path/extension signal the blanket rule keys off. Drew the parallel to the trending post's automated weekly-disable pattern: a rule that infers "safe to ignore" from a cheap proxy (folder location; 7-day usage count) structurally cannot distinguish a legitimately quiet file/plugin from a load-bearing one that merely looks quiet by the same signal — the gap was only caught by manually rereading `git log --all -- drafts/`, not by an automated audit. Distinct from the 2026-07-18 "gitignore ate my drafts" post (that discovered the silent-no-op bug itself) — this is about the exception-carving decision that came right after, as a case study for why blanket pattern-matched policies need a named override channel.
  - Tags: ai/productivity/python/devtools; git/mcp/python/devtools. Sources: `drafts/publishing-log-distinct-claims-never-checked.md`, `drafts/gitignore-blanket-rule-one-file-exception.md`.
  - https://dev.to/enjoy_kumawat/my-publishing-log-claims-every-post-is-distinct-i-never-checked-if-readers-agree-5ch
  - https://dev.to/enjoy_kumawat/my-gitignore-had-a-blanket-rule-one-file-broke-it-and-no-pattern-would-have-caught-that-5bmo

### 2026-07-21 - DEV.to: 2 articles published (first run of the day)
- **Status**: Completed (both live; verified via HTTP 200 fetch of each URL)
- **Description**: Session started with HEAD detached from `main` again (same recurring provisioning symptom, `bugs.md` 2026-07-18), 12 commits behind `main`/`origin/main` — recovered with `scripts/sync-main.sh` before any git-writing work, fast-forwarding cleanly onto `origin/main` (8 files, including the comment-reply pipeline from yesterday's runs), no divergence. Quota check showed 0 published today (new UTC day; last entry was 2026-07-20T12:37 UTC), so this was the first run of the day; published 2 per the normal 1-3 range. Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments. Explicitly excluded top scorers already mined or off-voice: "8 Things Developers Confidently Explain After Watching One YouTube Video" (off-voice joke listicle, top score), "Every AI-Generated Line of Code Is a Small Loan" (used 2026-07-18 as source trend), "I Stopped Debugging at My Desk" (used 2026-07-17), "Stratagems #15/#18" (off-voice fiction series), "Six experiments on adversarial verification" (repeatedly used prior source trend), "Stop Saying You Want Ownership Mindset" (off-voice career advice), "Building an AI Agent That Knows When Not to Guess" (repeatedly used prior source trend), "How I made a Rust hot path 27x faster..." (used 2026-07-19 second run), "Instrumenting an AI-Powered GitHub Analyzer with OpenTelemetry and SigNoz" and "I got tired of not knowing what my AI agents were doing..." (both used as source trends 2026-07-20 second run), "A Dash of dev.to: My Blog Stats Now Live in the Terminal" (used 2026-07-20 second run as source trend), "My extension 'worked'..." (used 2026-07-17), "What is an 'agentic harness,' actually?" (used 2026-07-19 first run), "Post-Mortem: Building a Local MCP Server...ChromaDB" (repeatedly judged too close to the FTS5-not-vectors post), "I built 8 security layers for an MCP marketplace" (used 2026-07-19 second run). Picked 2 themes with a distinct first-hand angle, both freshly mined from previously-unused real incidents in this repo's own logs:
  1. "AI And Code Ownership: Who Is Responsible For Generated Code?" trend (ai/career/programming, score 117) → reframed around the real, previously-unwritten 2026-07-19 `bugs.md` incident: a post-publish stop hook flagged 8 pushed commits as "Unverified" on GitHub and prescribed `git config user.name Claude` + `git commit --amend --reset-author` / `git rebase --exec` to fix it. Investigated before acting: 6 of the 8 commits already had `noreply@anthropic.com` as committer (the real gap was a missing signature, not identity), the target was already-pushed shared history requiring a force-push this session has no standing authorization to perform unattended, and setting committer identity to `Claude` is AI attribution moved into git metadata instead of commit-message text — the same rule this repo already enforces mechanically for message text (`_STRIP` filter, see the 2026-07-15 "kept signing its own work" post). Declined to run the fix; logged it in `bugs.md` instead. Distinct from the "kept signing its own work" post (natural-language instruction not self-enforcing for message text) and the "285 lines of docs" post (accepting reviewer feedback) — this is about a *hook's* prescribed fix crossing into destructive shared-history rewriting plus AI-identity attribution, and correctly refusing to auto-execute it.
  2. "I measured every millisecond of my real-time AI pipeline. The LLM was the fast part." trend (ai/llm/performance/webdev, score 18) → reframed around this exact publishing pipeline, timed live during this run: Step 1's quota check + Step 2's 6-tag trending fetch (7 GET requests total) completed in ~2.6s combined (0.91s quota check, 0.26-0.32s per tag), while the mandatory 35s inter-publish spacing (required after the 2026-06-29 and 2026-07-04 batches both hit live `429`s from rapid POSTs) makes up roughly 70% of a 3-article run's wall-clock floor — dwarfing both the read phase and the actual POST calls. Distinct from the 2026-07-16 "retry vs read the error" post (that was about *classifying* 429-vs-401/403/404 to decide whether to retry at all) — this is about *measuring* how much of total run time the already-correct retry/spacing logic actually consumes, which nobody had put a clock on before.
  - Tags: git/ai/security/devtools; ai/python/debugging/productivity. Sources: `drafts/stop-hook-told-me-to-rewrite-history.md`, `drafts/publishing-pipeline-bottleneck-is-not-the-ai.md`.
  - https://dev.to/enjoy_kumawat/a-post-commit-hook-told-me-to-rewrite-8-pushed-commits-to-fix-unverified-i-said-no-19f0
  - https://dev.to/enjoy_kumawat/i-timed-every-stage-of-my-ai-publishing-pipeline-the-ai-wasnt-the-slow-part-efl

### 2026-07-21 - DEV.to: 8 new comment replies drafted
- **Status**: Completed
- **Description**: Ran `reply_comments.py pending` — session started in detached HEAD again (recurring provisioning symptom, `bugs.md` 2026-07-18), recovered by checking out `main` and fast-forwarding onto `origin/main` before any git-writing work. 8 pending comments found across 5 articles, all from `alexshev`, `mads_hansen_27b33ebfee4c9`, and `fromzerotoship`. Fetched each article's live `body_markdown` via the DEV.to API to ground replies in the actual published text rather than the log summaries alone (article drafts themselves are gitignored per ADR-005, so the source markdown files aren't present in a fresh clone). Notable ones: on "My AI Commit Script and My MCP Tool Run the Exact Same Code," a commenter's point that `-> str` alone doesn't solve the empty-diff failure sent me back to `server.py` — confirmed `generate_commit_message` has no empty-diff guard at all (the standalone script does), so the reply corrects the article's implicit claim instead of just agreeing with it. On "My Comment-Reply Bot Hit a Wall," a commenter correctly pushed back that inferring API boundaries from two failure codes isn't the same as verifying against a spec — replied acknowledging that gap rather than defending the original inference. Drafted all 8 in `drafts/comment_replies.md`.
- **Status**: Completed (both live; verified via HTTP 200 fetch of each URL)
- **Description**: Session started with HEAD detached from `main` again (same recurring provisioning symptom, `bugs.md` 2026-07-18) — recovered with `scripts/sync-main.sh` before any git-writing work; local `main` fast-forwarded 10 commits onto `origin/main`, no divergence (confirmed via `git fetch` + `rev-parse` that both refs matched exactly — the "ahead by 10" message right after the merge was stale tracking info, not real divergence). Quota check showed 2 published today already (from this morning's first run), well under the 5-article cap, so this run published 2 more per the normal 1-3 range, landing today's total at 4. Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments. Explicitly excluded top scorers already mined or off-voice: "8 Things Developers Confidently Explain After Watching One YouTube Video" (off-voice joke listicle, top score), "The Myth of the Post-Documentation Era" (already this repo's own post/judged too close on multiple prior runs), "Every AI-Generated Line of Code Is a Small Loan" (used as this morning's source trend), "I Stopped Debugging at My Desk" (used 2026-07-17), "Stratagems #15"/"Stratagems #18" (off-voice fiction series), "Six experiments on adversarial verification" (repeatedly used prior source trend), "Stop Saying You Want Ownership Mindset" (off-voice career advice), "Building an AI Agent That Knows When Not to Guess" (repeatedly used, already adjacent to this morning's post), "How I made a Rust hot path 27x faster..." (used 2026-07-19 second run), "My extension 'worked'..." (used 2026-07-17), "What is an 'agentic harness,' actually?" (used 2026-07-19 first run), "A Vibe Is Not a Verdict" and "Post-Mortem: Building a Local MCP Server...ChromaDB" (both repeatedly judged too close to prior posts), "I built 8 security layers for an MCP marketplace" (used 2026-07-19 second run), "AI Agent Cost Drift" (used 2026-07-18 as source trend). Also skipped several posts that were this repo's own prior articles resurfacing in its own trending feed (e.g. "My Agent Kept Saying 'Tests Pass.'", "My Open Source PR Added 285 Lines of Docs.", "My Commit Message Generator Kept Signing Its Own Work."). Picked 2 themes with a distinct first-hand angle, both grounded in a genuinely new gap found by rereading this exact repo's own code rather than a prior incident already written up:
  1. "A Dash of dev.to: My Blog Stats Now Live in the Terminal" trend (productivity, score 59) → reframed around a real, verified-live gap in this exact publishing pipeline: `server.py`'s `list_articles`/`get_article_stats` MCP tools implement exactly the reflexive scoring this task's own Step 2 formula (`reactions + 3*comments`) would need to apply to my own 30+ published posts, but a repo-wide grep confirmed zero callers — the formula has scored 100+ other people's trending posts every run and never once scored my own output. Distinct from the "20 unanswered comments" post (that was a reactive gap — responding to engagement that already arrived) and the "memory file cost drift" post (a file getting more expensive to read under a contract that already existed) — this is a measurement contract that was never written at all.
  2. "Instrumenting an AI-Powered GitHub Analyzer with OpenTelemetry and SigNoz" (ai, score 95) reinforced by "I got tired of not knowing what my AI agents were doing, so I built a tiny observability..." (ai, score 31) → reframed around the real 2026-07-14 blocked-run incident: diagnosing the `dev.to:443` egress denial required manually curling `$HTTPS_PROXY/__agentproxy/status` from outside, because `server.py`'s `_gh`/`_dev` HTTP helpers (shared by all 8 tools) log nothing on success or failure. Grounded in the actual helper code, a 4-line wrap-after-definition decorator that writes one structured JSON line per call, and an explicit warning against logging request headers (both helpers send bare credentials in an `Authorization`/`api-key` header — a naive "log everything on failure" version would leak them to a plaintext file). Distinct from the detached-HEAD and gitignore-drafts posts (both diagnosed a symptom from outside after the fact for code this session doesn't own) — this is about code the author owns end-to-end still requiring an external probe because it kept no record of its own.
  - Tags: ai/productivity/python/devtools; mcp/python/debugging/devtools. Sources: `drafts/my-scoring-formula-never-scores-itself.md`, `drafts/eight-mcp-tools-zero-log-lines.md`.
  - https://dev.to/enjoy_kumawat/my-blog-posting-agent-scores-every-trending-post-it-reads-it-has-never-scored-one-of-its-own-2540
  - https://dev.to/enjoy_kumawat/my-mcp-server-has-8-tools-and-zero-log-lines-diagnosing-a-failure-meant-guessing-from-the-outside-4bh4

### 2026-07-20 - DEV.to: 2 articles published (first run of the day)
- **Status**: Completed (both live; verified via HTTP 200 fetch of each URL)
- **Description**: Session started with HEAD detached from `main` again (same recurring provisioning symptom, `bugs.md` 2026-07-18) — recovered with `scripts/sync-main.sh` before any git-writing work; local `main` fast-forwarded cleanly onto `origin/main`, no divergence. Quota check showed 0 published today, so this was the first run; published 2 of the normal 1-3 range. Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments. Explicitly excluded "Six experiments on adversarial verification — and the 75% wall that didn't move" (agents/llm, top scorer) as too close to the 2026-07-16 "precision stopped improving" post; excluded "I Could Review It. I Couldn't Write It." and "The Myth of the Post-Documentation Era" (both already judged too close to this repo's own posts per the 2026-07-19 log); excluded "Building an AI Agent That Knows When Not to Guess" and further MCP-security trends as repeatedly-mined prior source trends. Picked 2 themes with a distinct first-hand angle:
  1. Restraint/abstention in scheduled automation (agents, adjacent to "knows when not to guess" trend but a different failure mode: not guessing vs. not padding to hit a quota) → grounded in this exact publishing pipeline's own quota logic: the daily-cap check, the trending-topic dedup against ~30 prior titles, the floor that forces 2 articles on a second run if the day is still at 0, and the real 2026-07-14 blocked run (dev.to egress denied) where the correct behavior was logging zero quota consumed rather than faking a count or forcing a fallback publish.
  2. Ephemeral-sandbox operational quirks (devtools/debugging), a sibling to the detached-HEAD root cause already documented in `bugs.md` 2026-07-18 but a distinct symptom: this session's own environment notes describe the writable disk as a fixed per-session quota where `df`'s "Avail" column can read 0 while "Used" is small, which reads exactly like a full host disk but isn't one and calls for the opposite response (delete caches immediately vs. escalate/wait). Grounded in `shutil.disk_usage`, a concrete preflight-check snippet, and the same commit-pinned-container root cause as the detached-HEAD bug.
  - Tags: ai/agents/python/devtools; python/devtools/debugging/agents. Sources: `drafts/agent-that-knows-when-to-skip-a-day.md`, `drafts/df-lied-about-my-disk.md`.
  - https://dev.to/enjoy_kumawat/my-blog-posting-agent-has-a-daily-quota-the-feature-that-mattered-was-the-one-that-skips-it-4fpe
  - https://dev.to/enjoy_kumawat/df-said-my-sandbox-had-no-disk-left-it-wasnt-wrong-it-just-wasnt-answering-the-question-i-asked-3l3g

### 2026-07-19 - DEV.to: 2 articles published (second run of the day)
- **Status**: Completed (both live; verified via HTTP 200 fetch of each URL)
- **Description**: Session started with HEAD detached from `main` again (recurring provisioning symptom, `bugs.md` 2026-07-18) — recovered with `scripts/sync-main.sh` before any git-writing work, fast-forwarding onto this morning's first-run commits. Quota check showed 2 published today already (from this morning's run), well under the 5-article cap, so this run published 2 more per the normal 1-3 range, landing today's total at 4. Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments. Explicitly excluded top scorers already mined or off-voice: "8 Things Developers Confidently Explain After Watching One YouTube Video" (off-voice joke listicle, top score of the batch), "I Could Review It. I Couldn't Write It." and "The Myth of the Post-Documentation Era" (both already this repo's own posts / judged too close), "I Stopped Debugging at My Desk" (used 2026-07-17), "Stratagems #15" (off-voice fiction), "Every AI-Generated Line of Code Is a Small Loan" and "What is an 'agentic harness,' actually?" (both used as this morning's own source trends), "Stop Saying You Want Ownership Mindset" (off-voice career advice), "Six experiments on adversarial verification" and "Building an AI Agent That Knows When Not to Guess" (both repeatedly used prior source trends), "A Vibe Is Not a Verdict" (already judged too close to the "knows when not to guess" trend), "Post-Mortem: Building a Local MCP Server...ChromaDB" (judged too close to the FTS5-not-vectors post on multiple prior runs), "My extension 'worked'..." (used 2026-07-17), "AI Agent Cost Drift" (used as a source trend 2026-07-18). Picked 2 themes with a distinct first-hand angle:
  1. "How I made a Rust hot path 27x faster, and the AI fix I refused to merge" trend (ai, score 90) → distinct from the earlier "functional doesn't mean correct" post (that was general test-passing-but-wrong correctness) — this one is about a first-instinct fix I almost shipped myself in the real `google-gemini/gemini-cli` #28090/#28401 work: a naive byte-slice truncation of shell output looks done and passes an ASCII-only test, but silently corrupts multi-byte UTF-16 surrogate pairs landing exactly at the cut boundary — no crash, no lint warning, just a mangled character sent to the model. Grounded in the real `truncateLlmOutput` codepoint-safe implementation, the 4 unit tests added, and the verified 40319→32768-byte case.
  2. "I built 8 security layers for an MCP marketplace. Here's what each one actually catches." trend (mcp, score 18) → distinct from all 4 prior MCP-security posts (2026-06-22 build-side server hardening, 2026-06-29 install-side vetting checklist, 2026-07-03 tool-schema bloat, 2026-07-15 untrusted-payload-through-a-trusted-pipe) — those all treat a server you control as the boundary; this one is about empirically probing a black-box third-party platform's (DEV.to) actual write-API boundary: `POST /api/comments` 404s while `POST /api/reactions` 401s with the same valid key — two different failure mechanisms (no route to evaluate vs. evaluated-and-rejected) that together confirm this is a deliberate design boundary, not a missing header. Grounded in the real `reply_comments.py` pipeline (2026-07-18) and `key_facts.md`'s documented API write-limits finding.
  - Tags: ai/agents/debugging/devtools; mcp/security/python/devtools. Sources: `drafts/gemini-cli-codepoint-safe-truncation.md`, `drafts/devto-api-write-wall-is-a-feature.md`.
  - https://dev.to/enjoy_kumawat/i-fixed-unbounded-shell-output-in-an-open-source-agent-my-first-draft-would-have-corrupted-text-3egp
  - https://dev.to/enjoy_kumawat/my-comment-reply-bot-hit-a-wall-the-docs-never-mentioned-that-wall-turned-out-to-be-a-security-bjm

### 2026-07-19 - DEV.to: 2 articles published (first run of the day)
- **Status**: Completed (both live; verified via HTTP 200 fetch of each URL)
- **Description**: Session started with HEAD detached from `main` again (1 unpushed local commit, `317c2ef` "docs: draft replies for 2 new dev.to comments", not yet on `origin/main`) — recurring provisioning symptom per `bugs.md` 2026-07-18. Recovered with `scripts/sync-main.sh` (fast-forwarded main, then `git push origin main` — turned out already up to date, the detached commit itself hadn't been pushed as a branch ref). Quota check showed 0 published today (new UTC day), so this run published 2 per the normal 1-3 range. Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments. Explicitly excluded top scorers already mined or off-voice: "The Citation Lied Without Lying" (repeatedly used prior source trend), "8 Things Developers Confidently Explain After Watching One YouTube Video" (off-voice joke listicle), "I Could Review It. I Couldn't Write It." and "The Myth of the Post-Documentation Era" (already this repo's own posts/judged too close), "Simple Benchmark Review: Ollama on Jetson Nano" and "Stop Saying You Want Ownership Mindset" (off-voice hardware/career-advice), "I Stopped Debugging at My Desk" (already used 2026-07-17), "Stratagems #15" (off-voice fiction), "Building an AI Agent That Knows When Not to Guess" and "Six experiments on adversarial verification" (both repeatedly used prior source trends), "Post-Mortem: Building a Local MCP Server...ChromaDB" (already judged too close to the FTS5-not-vectors post), "AI Agent Cost Drift" and "MCP for TypeScript Developers" (both used as source trends 2026-07-18). Picked 2 themes with a distinct first-hand angle, both freshly mined from the comment-reply pipeline built yesterday (2026-07-18) and this repo's own MCP-tool-vs-script duality — neither previously written up as its own article:
  1. "Every AI-Generated Line of Code Is a Small Loan — And Eventually, You Have to Pay It Back" trend (productivity, score 130) → reframed around the real 2026-07-18 discovery: 20 unanswered comments across 35 published articles, some 6 weeks old, found only by accident while poking at an unrelated API endpoint — the publishing loop's contract never included "check what happens after publish," so the debt was invisible to the system that created it. Grounded in `reply_comments.py`'s real dedup logic (`replied_by_me()`'s recursive child-walk, the drafted-vs-on-site double dedup in `pending()`). Distinct from the "memory file cost drift" post (that was a file growing under an existing read-it-every-run contract) and the "twice-daily quota, no shared state" post (that was about coordination via an external system of record) — this is about a category of state the system never had a contract to check at all.
  2. "What is an 'agentic harness,' actually?" trend (llm, score 55) → reframed around a real side-by-side in this repo: `git_commit.py` (a 20-line manual script) and `generate_commit_message` (an MCP tool in `server.py`) call the identical `claude -p` subprocess with near-identical prompts — the only structural difference is `@mcp.tool()` turning the function signature into a schema an agent can read without executing it first. Initially assumed the MCP version must have better safety handling (a stripped-attribution filter); verified by rereading `server.py` and found this was **wrong** — both paths apply the identical `_STRIP` filter, just structured differently: the script duplicates it inline, the tool inherits it for free via the shared `_claude()` helper every tool routes through. Corrected the draft before publishing rather than let the plausible-but-false claim stand. Distinct from the earlier FastMCP-decorators post (that was about the decorator library's schema-generation gaps) and the tool-schema-bloat post (upfront token cost) — this is about what "agentic" means at the interface-boundary level when the underlying call is provably identical code.
  - Tags: ai/productivity/python/devtools; mcp/claudecode/python/devtools. Sources: `drafts/twenty-unanswered-comments-nobody-told-me.md`, `drafts/same-code-one-is-an-agent.md`.
  - https://dev.to/enjoy_kumawat/my-blog-had-20-unanswered-comments-across-35-posts-nothing-ever-told-me-5g6
  - https://dev.to/enjoy_kumawat/my-ai-commit-script-and-my-mcp-tool-run-the-exact-same-code-only-one-of-them-is-agentic-b50

### 2026-07-19 - DEV.to: 2 new comment replies drafted
- **Status**: Completed
- **Description**: Ran `reply_comments.py pending` — session started in detached HEAD again (recurring provisioning symptom, see `bugs.md` 2026-07-18), recovered with `scripts/sync-main.sh` before any git-writing work. 2 pending comments found, both from `alexshev`: one on "I Almost Hand-Rolled JSON-RPC for an MCP Server." (agreeing that boring MCP plumbing frees up taste for tool boundaries — replied with the two concrete gaps FastMCP's decorators didn't cover for free: the `min(per_page, 30)` clamp and opaque tool-call error formatting), one on "I Measured What My Agent's Own Memory File Costs to Read." (proposing periodic distillation as the fix — replied that this repo's own `bugs.md`/`decisions.md`/`key_facts.md` vs. raw `issues.md` split is already an informal version of that pattern, and named the actual gap: staleness detection in the distilled files is still manual). Drafted both in `drafts/comment_replies.md`.

### 2026-07-18 - DEV.to comment-reply pipeline set up; 20 backlog replies drafted
- **Status**: Completed
- **Description**: All 35 published articles had 20 total comments, none replied to. Verified the dev.to API cannot post comments (404) or reactions (401 for normal users), so full auto-reply is impossible via API — built a draft-only pipeline instead. Added `reply_comments.py` (`pending` → JSON of top-level comments not replied to on-site and not yet in `drafts/comment_replies.md`). Drafted replies for the entire 20-comment backlog in `drafts/comment_replies.md` (paste manually via each comment link, then delete the entry). Created cloud routine "DEV.to comment reply drafter" (trig_017t2Qn9abRsjNLjp8iGVCko, daily 02:30 UTC / 8:00am IST) that drafts replies for new comments and commits them, mirroring the publisher routine's environment. Note: `drafts/comment_replies.md` is un-ignored as a deliberate exception to ADR-005 — it's working state the cloud routine must read/write, not a duplicate of published content.

### 2026-07-18 - DEV.to: 2 articles published (second run of the day)
- **Status**: Completed (both live; verified via HTTP 200 fetch of each URL)
- **Description**: Session start found HEAD detached from `main` again (3 commits ahead, all pure additions to `issues.md`, already present on `origin/main`) — the same recurring symptom as 2026-07-15, 2026-07-16, and this morning's first run. Recovered with `git checkout main && git merge --ff-only`, then investigated *why* it keeps recurring instead of just fixing it again. Quota check showed 2 published today (from this morning's run), well under the 5-article cap, so this run published 2 per the normal 1-3 range, landing today's total at 4. Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments. Explicitly excluded top scorers already mined or off-voice: "The Citation Lied Without Lying" and "Six experiments on adversarial verification" (prior source trends), "The Myth of the Post-Documentation Era" and "I Could Review It, I Couldn't Write It" (already this repo's own posts / judged too close), "8 Things Developers Confidently Explain After Watching One YouTube Video" and "Stratagems #15" (off-voice: joke listicle / fiction), "Simple Benchmark Review: Ollama on Jetson Nano" and "Stop Saying You Want Ownership Mindset" (off-voice: hardware benchmark / career advice), "Building an AI Agent That Knows When Not to Guess" (already used as a source trend), "Post-Mortem: Building a Local MCP Server...ChromaDB" (already judged too close to prior posts on 2026-07-18), "AI Agent Cost Drift" (already used as this morning's source trend), "My extension 'worked' — it just quietly saved half of every conversation" (already used 2026-07-17). Instead of forcing a trend match, mined two real incidents surfaced by this run's own git recovery and a fresh audit of a task instruction, both genuinely new:
  1. Live incident from this run's own Step 0 (git housekeeping): re-diagnosed the recurring detached-HEAD bug. The 2026-07-15 article blamed concurrent agents sharing one working tree and prescribed `git worktree` isolation — but this is a single resumed session, not concurrent ones, and the theory never explained why the detached commits were already safely on `origin/main` (a push succeeding, not racing). Root cause is environment provisioning checking out a specific commit SHA rather than the `main` ref, independent of anything this repo's code does. Wrote `scripts/sync-main.sh` (idempotent, only ever fast-forwards, refuses to touch divergent history) and a corrected `bugs.md` entry. Distinct from the original worktree post — this one is about re-testing a published diagnosis against its own recurrence and finding the fix targeted the wrong layer, not about concurrent-session isolation.
  2. Real incident found while auditing this very task's Step 5 instruction ("commit drafts + the log"): `git log --all -- drafts/` is empty across 30+ published articles — `drafts/` has been gitignored since the repo's first commit, so every prior "committed drafts" claim silently only ever committed the log entry. Verified live (`git add drafts/<file>` exits 0 while printing an ignored-path hint). Turned the accidental gap into a deliberate one via `decisions.md` ADR-005 (drafts stay local/ephemeral; the log entry + live URL is the permanent record) rather than force-adding a growing pile of duplicate markdown into history. Distinct from the prior "silently swallowed errors" post (`2>/dev/null` hiding a real failure) — this is a command that isn't hiding anything; it's honestly reporting success for a smaller scope than assumed, and nobody had checked `git status` against the claim.
  - Tags: git/ai/debugging/devtools; git/ai/agents/devtools. Sources: `drafts/gitignore-ate-my-drafts-folder.md`, `drafts/detached-head-recurrence-wrong-diagnosis.md`.
  - https://dev.to/enjoy_kumawat/my-publishing-task-said-commit-the-drafts-my-gitignore-had-other-plans-3jnp
  - https://dev.to/enjoy_kumawat/i-already-wrote-the-article-fixing-this-bug-it-broke-again-anyway-54ne

### 2026-07-18 - DEV.to: 2 articles published (first run of the day)
- **Status**: Completed (both live; verified via HTTP 200 fetch of each URL)
- **Description**: Session start found HEAD detached with 2 unreachable commits from the 2026-07-17 second run (`docs: log 2 dev.to publishes (first/second run of the day)`, hashes `fc0b2a3`/`8882c73`) — a prior session had committed that day's log entries in a detached-HEAD state and never merged them back onto `main`, so they were never pushed. Recovered them with `git merge --ff-only` onto `main` (pure additions to `issues.md`, no conflicts) before adding this run's entry, so that log history isn't lost. Quota check showed 0 published today (30+ total historical, none from 2026-07-18), so this run published 2 per the normal 1-3 range. Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments. Explicitly excluded top scorers already mined or off-voice: "8 Things Developers Confidently Explain After Watching One YouTube Video" (off-voice joke listicle), "The Citation Lied Without Lying" and "Six experiments on adversarial verification" (both prior source trends), "The Myth of the Post-Documentation Era" and "I Could Review It, I Couldn't Write It" (already this repo's own posts / judged too close), "Simple Benchmark Review: Ollama on Jetson Nano" and "Stop Saying You Want Ownership Mindset" (off-voice: hardware benchmark / career advice), "Stratagems #15" (off-voice fiction), "I Stopped Debugging at My Desk" and "Building an AI Agent That Knows When Not to Guess" (both already used as source trends on 2026-07-17), "A Vibe Is Not a Verdict" and "Post-Mortem: Building a Local MCP Server...ChromaDB" (already judged too close to prior posts). Picked 2 themes with a distinct first-hand angle, both freshly mined from previously-untouched project sources rather than the same bugs.md entries used before:
  1. "AI Agent Cost Drift: 0.35%/day Is Invisible to Your Dashboard" trend (agents tag, score 16 — description explicitly named "system prompt, tool schemas, CLAUDE.md" as the input floor) → reframed around a genuinely new angle on this repo's own `docs/project_notes/issues.md`: measured its real size (29,446 bytes / ~7,360 tokens, 21 entries, growing ~2 entries/day with per-entry length itself increasing as the "already excluded" rationale list grows against entry count). Distinct from the prior "56 KB context flooding" post (that was one bad runtime tool call dumping a big file once; this is a file the agent is *supposed* to read in full every run, so "read less" isn't a valid fix) — the article works through why that makes it a different problem and sketches (not yet implemented) an archive-split fix: rolling window + one-line index for older entries.
  2. "Claude Code, Beyond the Prompt — Part 4: Your First MCP Server" (mcp tag, score 84) and "MCP for TypeScript Developers: What It Actually Solves Beyond the Hype" (agents tag, score 13) → distinct from all 3 prior MCP posts (schema-bloat=upfront token cost, build-side security=prompt injection defenses, install-side vetting=pre-install checklist) — this one is a framework-choice piece grounded in `decisions.md` ADR-002 (FastMCP decorators over hand-rolled low-level JSON-RPC) and real code from `server.py`'s `get_repo_stats`/`list_articles` tools, including two real gaps the decorator doesn't cover for free (the `min(per_page, 30)` validation FastMCP's schema generation doesn't enforce on its own, and opaque tool-call errors needing explicit reformatting).
  - Tags: ai/agents/productivity/devtools; mcp/python/claudecode/devtools. Sources: `drafts/my-memory-file-is-the-cost-drift.md`, `drafts/fastmcp-decorator-vs-hand-rolled-jsonrpc.md`.
  - https://dev.to/enjoy_kumawat/i-measured-what-my-agents-own-memory-file-costs-to-read-the-number-only-goes-up-46ob
  - https://dev.to/enjoy_kumawat/i-almost-hand-rolled-json-rpc-for-an-mcp-server-eight-tools-later-im-glad-i-didnt-17mj

### 2026-07-17 - DEV.to: 2 articles published (second run of the day)
- **Status**: Completed (both live; verified via HTTP 200 fetch of each URL)
- **Description**: Quota check showed 2 published today already (from this morning's first run), well under the 5-article cap, so this run published 2 more per the normal 1-3 range, landing today's total at 4. Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments. Explicitly excluded top scorers already mined or off-voice: "The Citation Lied Without Lying" and "Six experiments on adversarial verification" (prior source trends), "The Myth of the Post-Documentation Era" and "I Could Review It, I Couldn't Write It" (already this repo's own posts / judged too close), "8 Things Developers Confidently Explain After Watching One YouTube Video" and the "Stratagems" fiction series (off-voice: joke listicle / fiction), "Simple Benchmark Review: Ollama on Jetson Nano" and "Stop Saying You Want Ownership Mindset" (off-voice: hardware benchmark / career advice), "Building an AI Agent That Knows When Not to Guess" and "A Vibe Is Not a Verdict" (the former already used as this morning's source trend, the latter too close to it), "Post-Mortem: Building a Local MCP Server... using Ollama and ChromaDB" (too close to the already-published FTS5-not-vectors post). Instead of forcing a second article onto a loosely-related trend, went back to `bugs.md`/the repo's own `hooks/` directory for genuinely unused real incidents, matched to nearby trending themes:
  1. Real incident from `bugs.md` (2026-07-01, never turned into its own article) — the `vercel/eve` pre-commit hook's `ENOENT: git` error while committing the #412 fix, root-caused to `scripts/pre-commit-fmt.mjs` computing its `cwd` via `new URL("..", import.meta.url).pathname` instead of `fileURLToPath()`, which keeps a bogus leading slash before the Windows drive letter. Distinct from all prior posts — this is a general Node/Windows path-resolution debugging lesson (ENOENT from `spawnSync` can mean "bad cwd," not just "command not found"), not previously covered from this repo's incident log. Loosely paired with the "I Stopped Debugging at My Desk" trend (productivity, score 127) — the real lesson is debugging by causal chain (what fed the failing call a bad input) vs. debugging by symptom location (the command that happened to throw).
  2. Real, previously-undocumented-as-an-article bug found by rereading this repo's own `hooks/prepare-commit-msg`: the `python "$SCRIPT" > "$1" 2>/dev/null || true` line correctly keeps a broken `claude` CLI call from ever blocking a commit, but as written it also makes "the AI call crashed" and "the AI call ran and had nothing to say" render identically — an empty message buffer, no error, no trace, `2>/dev/null` throwing the reason away. Matched to the "My extension 'worked' — it just quietly saved half of every conversation" trend (productivity, score 46) — same shape of bug: a success signal that's actually silence hiding failure. Fix shown: keep the always-succeed guarantee but write the captured error into the message buffer as `#`-prefixed lines (git ignores them in the real commit, but they're visible in the editor) instead of swallowing it. Distinct from the prior "kept signing its own work" post (that was about a natural-language instruction not being self-enforcing for AI-attribution stripping) — this is about a shell hook's own failure-signaling design, unrelated to what the AI outputs when it succeeds.
  - Tags: debugging/javascript/git/devtools; ai/git/debugging/devtools. Sources: `drafts/enoent-git-was-never-the-problem.md`, `drafts/pre-commit-hook-silently-swallowed-failures.md`.
  - https://dev.to/enjoy_kumawat/my-git-commit-failed-with-enoent-git-git-was-never-the-problem-c1p
  - https://dev.to/enjoy_kumawat/my-ai-commit-hook-never-failed-a-single-commit-that-was-the-bug-2933

### 2026-07-17 - DEV.to: 2 articles published (first run of the day)
- **Status**: Completed (both live; verified via HTTP 200 fetch of each URL)
- **Description**: Quota check showed 0 published today (27 total historical, none from 2026-07-17), so this run published 2 per the normal 1-3 range. Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments. Explicitly excluded top scorers already mined or off-voice: "The Citation Lied Without Lying" and "Six experiments on adversarial verification" (both prior source trends), "I Could Review It, I Couldn't Write It" (already judged too close to reviewer-fatigue/verification posts), "The Myth of the Post-Documentation Era" (already the docs-for-an-agent post), "Claude Code worktrees" and "My Project's Own Instructions..." (both this repo's own prior posts resurfacing in its own trending feed), "Teaching a Qwen agent to forget" and "The AI orientation tax" (too close to the stale-memory and tools-that-don't-exist posts), plus several off-voice items (joke listicles, hardware benchmarks, career-advice pieces, fiction). Picked 2 themes with a distinct first-hand angle:
  1. "Alberta Ran 50 AI Agents in Parallel. Everyone Shared the Same Number." trend (agents tag, score 42) reinforced by "An Agent That Hunts Bugs in My App While I Sleep" (agents, score 25) → reframed around this exact routine's own coordination design: two scheduled runs a day share a daily cap (5) and a "still 0, publish at least 2" rule with zero shared process, filesystem, or local counter between them — the only shared state is DEV.to's own `/me/published` API, re-queried fresh every run. Grounded in the real 2026-07-14 incident where a run was blocked entirely by an egress denial and correctly consumed zero quota because there was no local state to corrupt. Distinct from the prior "multi-agent teams" post (fan-out/pipeline decision-making) and the "worktrees" post (git isolation) — this is about cross-invocation coordination via an external system of record, not intra-session parallelism.
  2. Real incident from `bugs.md` (2026-06-21) and `decisions.md` ADR-003, not itself a live trend but grounded in the "knows when not to guess" theme (ai tag, "Building an AI Agent That Knows When Not to Guess", score 95) → wrote up the `GITHUB_USERNAME` env var that silently held the DEV.to username instead, causing 404s, and why the fix was removing the ambiguity (two explicitly named constants) rather than adding validation/fallback logic to guess correctly. Distinct from the "stale memory file" and "docs for an agent" posts — this is about one config slot doing two jobs, not about facts going out of date or documentation style.
  - Tags: ai/agents/productivity/devtools; ai/python/devtools/debugging. Sources: `drafts/scheduled-agent-shared-quota-no-memory.md`, `drafts/one-env-key-two-usernames.md`.
  - https://dev.to/enjoy_kumawat/my-publishing-agent-runs-twice-a-day-and-remembers-nothing-thats-the-design-not-the-bug-11c7
  - https://dev.to/enjoy_kumawat/one-env-key-held-two-different-usernames-my-fix-wasnt-to-make-the-code-guess-better-28fp

### 2026-07-16 - DEV.to: 2 articles published (second run of the day)
- **Status**: Completed (both live; verified via HTTP 200 fetch of each URL)
- **Description**: Quota check showed 2 published today already (from this morning's first run), well under the 5-article cap, so this run published 2 more per the normal 1-3 range, landing today's total at 4. Also fixed HEAD being left detached from a prior session (fast-forwarded cleanly onto `main`, no lost work). Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments. Explicitly excluded top scorers already mined: "The Citation Lied Without Lying" and "Six experiments on adversarial verification" (both prior source trends), "Return on Attention" and "I Could Review It, I Couldn't Write It" (both already judged too close to the reviewer-fatigue/verification posts), "Claude Code worktrees" (already this repo's own worktree post), and the repo's own "instructions told my agent to use tools that don't exist" post showing up in its own trending feed. Picked 2 themes with a distinct first-hand angle:
  1. Real incident from this run's own Step 1: `dev.to:443` was previously blocked by sandbox egress policy (2026-07-14 log entry) and never turned into an article on its own. Reframed around the general lesson — retry logic needs to classify failures as transient (timeout, 5xx, 429 — retry with backoff) vs structural (401/403/404 from a policy or auth decision — retrying never changes the outcome), grounded in the actual `$HTTPS_PROXY/__agentproxy/status` diagnostic and this repo's own git-push retry convention (which already scopes retries to "network errors" only). Distinct from the "tests pass" and adversarial-verification posts — this is about classifying failures before reacting, not about verifying claimed success.
  2. Real incident found while writing this run: the repo had two live, functional dev.to-publishing scripts — `post_article.py` (old: hardcoded `published: False`, hardcoded tags, missing the `User-Agent` fix) and `publish_devto.py` (current: frontmatter-driven, has the UA fix). Wrote up why a stale-but-functional duplicate is a bigger risk for an agent (which matches by content/structure via grep, with no visible "deprecated" signal) than for a human (who'd notice via git blame/README friction) — distinct from the "unused MCP plugins eating context" trend since this is about literal duplicate scripts as landmines, not protocol-level tool schemas loaded every session. Fixed it for real: deleted `post_article.py`, updated `key_facts.md`'s file table.
  - Tags: ai/agents/debugging/python; ai/python/devtools/debugging. Sources: `drafts/retry-vs-read-the-error.md`, `drafts/two-scripts-one-api.md`.
  - https://dev.to/enjoy_kumawat/my-agent-almost-retried-a-request-that-was-never-going-to-succeed-2gm3
  - https://dev.to/enjoy_kumawat/my-repo-had-two-scripts-that-hit-the-same-api-only-one-still-matched-what-i-actually-do-10j4

### 2026-07-16 - DEV.to: 2 articles published (first run of the day)
- **Status**: Completed (both live; verified via HTTP 200 fetch of each URL)
- **Description**: Quota check showed 0 published today (23 total historical, none from 2026-07-16). Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments; picked 2 themes with a genuinely distinct angle, holding back a weaker productivity-tag candidate ("Return on Attention: AI code review fatigue") per the quality bar since its theme overlapped too closely with the verification-wall piece below:
  1. Meta/tooling trend: this repo's own `CLAUDE.md` "MANDATORY routing rules" section references `ctx_fetch_and_index`/`ctx_execute`/`ctx_batch_execute` MCP tools and blocks `curl`/`WebFetch` — none of which are wired up in this environment. Distinct from the prior "MCP tool-schema bloat" and "unbounded MCP dependency" posts (those were about MCP servers you *do* have; this is about a project instruction file describing tooling that *isn't there*, and the "MANDATORY" framing that discourages an agent from checking). Grounded first-hand in the actual CLAUDE.md text in this repo and the routine's own environment note telling the agent to ignore those rules because "those tools/hooks do not exist here."
  2. "Six experiments on adversarial verification — the 75% wall that didn't move" trend (llm/agents, score 74) → distinct from the earlier "stopped believing tests pass" post (that was about forcing a red-before/green-after transition, not about verification precision plateauing). Grounded in the Workflow tool's own documented adversarial-verify pattern (N identical refuter votes, majority kill) vs. perspective-diverse verify (distinct lenses per verifier) — walked through why identical redundant voters share the same blind spot and plateau, and why swapping to correctness/reproducibility/impact lenses moves the needle instead of adding more votes.
  - Tags: claudecode/ai/agents/devtools; ai/llm/agents/testing. Sources: `drafts/claude-md-told-me-to-use-tools-that-dont-exist.md`, `drafts/adversarial-verification-hits-a-wall.md`.
  - https://dev.to/enjoy_kumawat/my-projects-own-instructions-told-my-agent-to-use-tools-that-dont-exist-1724
  - https://dev.to/enjoy_kumawat/i-added-more-verifiers-to-catch-bad-ai-findings-precision-stopped-improving-26a5

### 2026-07-15 - DEV.to: 2 articles published (second run of the day)
- **Status**: Completed (both live; verified 200 on each URL)
- **Description**: Checked quota via `GET /api/articles/me/published` — today's total was already 2 (from this morning's first run), well under the 5-article cap, so this run published 2 more per the normal 1-3 range, landing today's total at 4. Scored trending ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments. Explicitly skipped top scorers already mined as source trends by prior posts: "The Citation Lied Without Lying" (already became the 2026-07-14 stale-memory-file post), "The Myth of the Post-Documentation Era" and the "detached HEAD" incident (both used by this morning's own run), "Return on Attention" and "I Deleted 200 Lines of Code I Didn't Write" (both already mined on 2026-07-14). Picked 2 themes with a distinct first-hand angle:
  1. "An alternative to LLM quality gates: deterministic routing + sampling" trend (ai/llm/agents/testing, score 166) → reframed around a real incident in this repo: `git_commit.py`'s `prepare-commit-msg` hook kept letting `claude -p`-generated commit messages slip in `Co-Authored-By: Claude` lines despite an explicit "never do this" system-prompt instruction (and despite the same rule being spelled out in `CLAUDE.md`). Fix was commit `1f8808c` — a deterministic post-generation line filter (`_STRIP` tuple + list comprehension), applied in both `git_commit.py` and `server.py`'s `_claude()` helper, that strips attribution lines regardless of what the model outputs. Distinct from the "tests pass" verification-discipline post (that was about not trusting self-reported success) and the "stale memory" post (that was about facts going out of date) — this is about not trusting a natural-language instruction to be self-enforcing at all, for constraints that can be checked mechanically.
  2. Real incident surfaced while running Step 2 of this very job (fetching trending dev.to posts by tag as source material) → distinct new angle on MCP security not covered by either of the two prior MCP-security posts (2026-06-22 build-side server hardening, 2026-06-29 install-side vetting checklist): those both treat "the server" as the trust boundary, but a fully-vetted, self-built server (`developer-presence`'s `get_repo_stats`/`get_repo_profile` tools, and this job's own trending-post fetch loop) still hands back free text authored by arbitrary third parties (repo descriptions, dev.to post titles/bodies) with no marker distinguishing it from a real instruction. Grounded in real code from `server.py` and this run's own `ctx`-style tag-scoring loop. Distinct because it's about the untrusted *payload* flowing through an already-trusted *pipe*, not about vetting the pipe itself.
  - Tags: ai/llm/git/devtools; mcp/security/ai/python. Sources: `drafts/deterministic-gate-not-llm-self-policing.md`, `drafts/tool-results-are-untrusted-data.md`.
  - https://dev.to/enjoy_kumawat/my-commit-message-generator-kept-signing-its-own-work-telling-it-not-to-wasnt-the-fix-2j3j
  - https://dev.to/enjoy_kumawat/my-mcp-server-only-talks-to-apis-i-trust-that-doesnt-mean-the-data-coming-back-is-trustworthy-1m0e

### 2026-07-15 - DEV.to: 2 articles published (first run of the day)
- **Status**: Completed (both live; verified 200 on each URL)
- **Description**: Checked quota via `GET /api/articles/me/published` — today's total was 0 (new UTC day), so this run published 2 per the normal 1-3 range. Scored trending ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments. Explicitly skipped several top scorers as already covered by the prior 19 posts: "Return on Attention: Why AI Code Reviews Are Wearing Us Out" and "I Could Review It. I Couldn't Write It." (both too close to the 2026-07-14 second-ai-reviewer-fatigue post), "Bigger Context Windows Didn't Make Our RAG Smarter" (too close to the FTS5-not-vectors and 56KB-context-flooding posts), "I Stopped Trusting the Agent's 'Done' - prove-it, a verify.sh Gate" (too close to the 2026-07-14 "tests pass" post), and the recurring "I Deleted 200 Lines of Code I Didn't Write" theme (already the undici-deletion-lesson post). Picked 2 themes with a distinct first-hand angle:
  1. "The Myth of the Post-Documentation Era" trend (ai/documentation, score 150) → reframed around a genuinely new angle: docs in `docs/project_notes/` are read by an agent every session, not by humans skimming a wiki, which changes what "good documentation" means (terse facts over narrative prose, expiry triggers on every fact, protocol blocks as routing logic). Grounded in the real stale `key_facts.md` token-scopes line (fixed 2026-07-14, caused a real repeat 403 six weeks after the underlying fix existed in `bugs.md`) and the actual `CLAUDE.md` Protocols block. Distinct from the earlier cross-session-memory post (that was about persistence; this is about writing style once you assume an LLM-only reader).
  2. Real incident from this run itself: `git status` on session start showed `HEAD detached from refs/heads/main` with 2 real commits from a prior session floating unreachable from any branch. Wrote it up live — root cause (a previous session checked out a specific commit instead of the branch ref and never switched back), the actual risk (concurrent sessions fighting over one shared working tree's `HEAD`), and the fix (`git worktree add` for isolated checkouts instead of switching branches in place, tying it to this environment's own `EnterWorktree`/`ExitWorktree` tools and the `Workflow` tool's `isolation: 'worktree'` option). Distinct from the multi-agent-teams post (that was about fan-out/pipeline decision-making, not git working-tree isolation). Weak trending signal on its own (claudecode-tag "worktrees" post, score 9) but strong first-hand anecdote that happened in this exact session.
  - Tags: ai/claudecode/documentation/productivity; claudecode/agents/git/devtools. Sources: `drafts/docs-for-an-agent-not-a-human.md`, `drafts/detached-head-agent-worktrees.md`.
  - https://dev.to/enjoy_kumawat/my-project-docs-arent-for-humans-anymore-theyre-for-an-agent-that-re-reads-them-every-session-56a7
  - https://dev.to/enjoy_kumawat/my-agents-git-checkout-left-two-commits-floating-in-nowhere-worktrees-fixed-the-actual-problem-54m2

### 2026-07-14 - DEV.to: 2 articles published (third run, capped at daily max of 5)
- **Status**: Completed (both live; verified 200 on each URL)
- **Description**: Checked quota via `GET /api/articles/me/published` (had to add `User-Agent: Mozilla/5.0` — dev.to 403s the default urllib UA on GET too, not just POST). Today's total was already 3 (from the second run's batch below), so this run published 2 to land exactly at the 5-article daily cap rather than the normal 1-3 range. Ranked trending ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments; explicitly excluded "The Citation Lied Without Lying," "The Agent Faked a Test Log," and the fastmcp/unbounded-dependency post since those were this morning's source trends already used. Picked 2 themes with a distinct first-hand angle not covered by the prior 17 posts:
  1. "Return on Attention: Why AI Code Reviews Are Wearing Us Out" trend (ai/productivity, score 124) → distinct from the "tests pass" verification-discipline post and the "citation lied" stale-memory post — this one is about the *workflow* cost of a mandatory second-AI-review step, grounded in this repo's own real `CLAUDE.md` line ("codex will review what you done") and a 3-bucket triage protocol (fix silently / ask first / skip silently), with real examples from the `vercel/eve` #454 DCO-signing decision (bucket: ask first) and the `nodejs/undici` #5446 review response (bucket: fix silently).
  2. "I Deleted 200 Lines of Code I Didn't Write and Learned More Than When I Wrote It" trend (ai, score 124) → distinct from all prior OSS-contribution mentions (that PR was never written up on its own) — grounded fully in the real `nodejs/undici` #5446 review-response event: added `Interceptors.md` duplicating an existing but hard-to-find `Dispatcher.md` section, reviewer caught it, fix was -285/+3 lines on commit `fbac28a1`. Lesson: keyword grep found the term but not the existing section's shape, since it was buried as a subsection of a differently-named file.
  - Tags: ai/claudecode/productivity/devtools; ai/agents/claudecode/debugging. Sources: `drafts/second-ai-reviewer-fatigue.md`, `drafts/undici-deletion-lesson.md`.
  - https://dev.to/enjoy_kumawat/every-commit-in-my-repo-gets-reviewed-by-a-second-ai-heres-what-actually-changed-4k92
  - https://dev.to/enjoy_kumawat/my-open-source-pr-added-285-lines-of-docs-a-reviewer-told-me-to-delete-most-of-them-they-were-3ii3

### 2026-07-14 - DEV.to: 3 articles published (second run, egress restored)
- **Status**: Completed (all 3 live; verified 200 on each URL)
- **Description**: First run today was blocked by a sandbox egress policy denial on `dev.to:443` (see entry below) — egress was restored by this run, and today's published count was still 0, so per the run policy this batch published 3 (minimum-2 floor cleared with room for a 3rd genuinely distinct angle). Ranked trending dev.to ai/llm/mcp/claudecode/agents/productivity posts by reactions + 3×comments; picked 3 themes with a distinct first-hand angle not covered by the prior 14 posts:
  1. "The Agent Faked a Test Log, Then Believed It" trend (agents/llm, score 132) → distinct from the earlier "faked tool result" post (that was a code bug in `vercel/eve` swallowing a terminal error) — this one is about verification *discipline*: never accept an agent's self-report of "tests pass" without a forced red-before/green-after transition, grounded in the actual eve #454 PR methodology and the repo's own `codex reviews what you've done` second-pass note.
  2. "The Citation Lied Without Lying" trend (agents/llm, score 131) → distinct from the earlier "agents don't remember" post (that was total amnesia) — this one is about *stale* memory being presented with full confidence. Found and fixed a real live instance in this repo: `key_facts.md`'s token-scopes line never got the `workflow` scope added after `bugs.md`'s 2026-06-23 entry documented needing it. Fixed both files as part of writing the piece.
  3. Real "unbounded MCP dependency" trend (mcp, fastmcp 421 incident) → audited this repo's own `requirements.txt`, found the exact same anti-pattern (`mcp[cli]` with zero version constraint), pinned it to `>=1.28.0,<2.0.0`. Also fixed a `publish_devto.py` bug found along the way: `load_env()` crashed with `FileNotFoundError` when no `.env` file exists (this sandbox has `DEV_TO_API` set directly in the environment) — added the same try/except pattern `server.py` already used.
  - Tags: agents/ai/claudecode/debugging; ai/llm/agents/productivity; mcp/python/security/devtools. Sources: `drafts/stopped-trusting-agent-done.md`, `drafts/citation-lied-without-lying.md`, `drafts/unbounded-mcp-dependency.md`.
  - https://dev.to/enjoy_kumawat/my-agent-kept-saying-tests-pass-i-stopped-believing-it-378k
  - https://dev.to/enjoy_kumawat/my-agents-memory-file-wasnt-wrong-it-was-just-six-weeks-stale-458m
  - https://dev.to/enjoy_kumawat/my-requirementstxt-had-a-landmine-in-it-it-just-hadnt-gone-off-yet-84n

### 2026-07-14 - DEV.to run blocked: dev.to unreachable from sandbox egress policy
- **Status**: Blocked (no articles published, no quota consumed)
- **Description**: Scheduled DEV.to publishing run could not get past Step 1 (quota check). `dev.to:443` is not on the environment's egress allowlist — every outbound HTTPS call to it fails at the agent proxy with a hard `403` on `CONNECT` (`gateway answered 403 to CONNECT (policy denial or upstream failure)`, confirmed via `curl $HTTPS_PROXY/__agentproxy/status`). This is a hard organization policy denial, not a transient network error — per `/root/.ccr/README.md` these must not be retried or routed around (no CA/proxy workaround applies; the host itself is denied). No workaround exists from inside this sandbox; the environment's network policy needs `dev.to` added to the allowlist for this to run.
- **Action needed**: Either run the DEV.to publishing task from an environment whose egress policy allows `dev.to`, or get `dev.to` added to this environment's allowed egress hosts.

### 2026-07-04 - DEV.to: 3 articles published (batch)
- **Status**: Completed (published live)
- **Description**: Ranked trending dev.to ai/llm/mcp/claudecode posts by reactions + 3×comments. Picked 3 distinct, first-hand topics not overlapping the prior 11 posts:
  - "faked tool result" trend → grounded in the real `vercel/eve` #412 fix (terminal `MODEL_CALL_FAILED` swallowed as silent success, missing `config.mode === "task"` check on the terminal branch).
  - "headless agent 401" trend → grounded in the real `ANTHROPIC_API_KEY`-vs-OAuth bug from `bugs.md` (raw API key assumed, actual auth is `claude -p` OAuth session).
  - "vector RAG weak for coding agents" trend → grounded in this repo's own `context-mode` tool, which indexes into an FTS5 knowledge base, not a vector DB.
  - dev.to rate-limited the 3rd POST (429 "try again in 30s"), same throttle as the 2026-06-29 batch; retried after cooldown.
  - Tags per article: claudecode/ai/agents/debugging; claudecode/ai/devops/debugging; mcp/ai/llm/python. Sources: `drafts/agent-faked-tool-result.md`, `drafts/headless-agent-401.md`, `drafts/fts5-not-vectors.md`.
  - https://dev.to/enjoy_kumawat/my-agent-said-the-tool-call-succeeded-it-had-404d-3c07
  - https://dev.to/enjoy_kumawat/the-token-was-valid-my-headless-agent-401d-anyway-3bgl
  - https://dev.to/enjoy_kumawat/i-ditched-vector-search-for-my-coding-agents-memory-fts5-won-22g2

### 2026-07-03 - DEV.to article: MCP tool schema bloat
- **Status**: Completed (published live)
- **Description**: Ranked trending dev.to mcp/ai/llm/claudecode posts by reactions + 3×comments. Picked "Your MCP servers are burning 50k+ tokens before you type a word" (mcp tag, live discussion) — distinct from the earlier "56KB context flooding" piece (that was runtime output bloat; this is upfront tool-schema bloat). Grounded first-hand in this session's own deferred-tool-loading pattern (ToolSearch: ~80 MCP tools listed by name only, full schema fetched on demand). Tags: mcp, ai, claudecode, productivity. Source: `drafts/mcp-tool-schema-bloat.md`.
- **URL**: https://dev.to/enjoy_kumawat/your-mcp-servers-are-burning-tokens-before-you-type-a-word-3076

### 2026-07-01 - OSS Contribution to vercel/eve — issue #412

- **Status**: Completed (PR opened, awaiting review)
- **Description**: Forked + cloned `vercel/eve` (already set up: fork `enjoykumawat/eve`, local clone `D:\codes\eve`). Root-caused #412 ("subagent MODEL_CALL_FAILED gets swallowed") via systematic-debugging: `packages/eve/src/harness/tool-loop.ts`'s `classification === "terminal"` branch returned `{ done: true, output: "" }` unconditionally, never checking `config.mode`, unlike the non-terminal branch right below it which already special-cased `config.mode === "task"` to set `isError: true`. Result: a subagent whose model call failed terminally (401/403/404/invalid API key/unresolvable model id) rolled up into the parent orchestrator via `workflow-entry.ts`'s `finalizeDone` as a **successful** empty-output `subagent-result` — matches the issue's exact repro (404 from an unresolvable OpenRouter model slug). Fix: applied the same `config.mode === "task"` check to the terminal branch. Added a regression test in `tool-loop.test.ts` (confirmed red before fix, green after). Full `packages/eve` unit suite run before/after — no new failures (pre-existing Windows path-separator failures in unrelated CLI/nitro-host tests present on `main` too). Added a changeset.
- **URL**: https://github.com/vercel/eve/pull/454
- **Notes**: No GPG/SSH signing key configured in this environment — vercel/eve requires verified signatures on protected branches (CONTRIBUTING.md); committed with DCO sign-off (`-s`) only, flagged in the PR for re-signing before merge. Also hit and worked around (didn't fix) an unrelated pre-existing Windows bug in the repo's own `scripts/pre-commit-fmt.mjs`: computes `REPO_ROOT` via `new URL("..", import.meta.url).pathname`, which keeps a leading `/` before the drive letter on Windows (`/D:/codes/eve/`), producing a bogus `cwd` for `spawnSync` that fails with a misleading `ENOENT: git` error on every commit. Worked around via the script's own `SKIP_SIMPLE_GIT_HOOKS=1` escape hatch after manually running `oxfmt`/`oxlint` to replicate what the hook would have done.

### 2026-06-29 - DEV.to: 4 articles published (batch)
- **Status**: Completed (all 4 live; verified in /me/published)
- **Description**: Ranked trending dev.to ai/llm/mcp/claudecode by reactions + 3×comments. Picked 4 in the user's first-hand lane, each distinct from prior 6 posts. dev.to throttles rapid POSTs (HTTP 429 "try again in 30s") — first batch loop published 2/4, retried the other 2 spaced ~35s apart. Voice: "real problem / my fix", concrete code each.
  1. "My AI Agent Read 56 KB to Answer One Question. I Made It Stop." (context flooding / context-mode sandbox routing; distinct from Context Rot=memory). Tags: ai, llm, claudecode, productivity. `drafts/agent-reads-56kb-context-flooding.md`
  2. "My AI Wrote Code That Passed Every Test and Was Still Wrong" (functional≠correct; r/c-top of week). Tags: ai, llm, codequality, claudecode. `drafts/functional-doesnt-mean-correct.md`
  3. "One Agent or Five? What I Learned Running a Team of AI Coders" (multi-agent fan-out vs pipeline). Tags: ai, claudecode, llm, productivity. `drafts/one-agent-or-many.md`
  4. "I Stopped Installing MCP Servers Blind. Here's My 5-Minute Vetting Checklist." (install-side MCP vetting; distinct from prior build-side security piece). Tags: mcp, ai, security, claudecode. `drafts/vetting-mcp-servers-before-install.md`
- **URLs**:
  - https://dev.to/enjoy_kumawat/my-ai-agent-read-56-kb-to-answer-one-question-i-made-it-stop-34g5
  - https://dev.to/enjoy_kumawat/my-ai-wrote-code-that-passed-every-test-and-was-still-wrong-lad
  - https://dev.to/enjoy_kumawat/one-agent-or-five-what-i-learned-running-a-team-of-ai-coders-4lci
  - https://dev.to/enjoy_kumawat/i-stopped-installing-mcp-servers-blind-heres-my-5-minute-vetting-checklist-30ph

### 2026-06-24 - DEV.to article: agent cross-session memory
- **Status**: Completed (published live)
- **Description**: Ranked trending dev.to AI/LLM/MCP/claudecode posts by reactions + 3×comments. Top fit for the user's lane: "agents don't remember" (high comment count = live problem), grounded first-hand in this repo's `docs/project_notes/` memory system. Distinct from prior "Context Rot" piece (that was mid-session degradation; this is cross-session amnesia). Voice: "real problem / my fix", concrete code = the 4-file structure + CLAUDE.md trigger protocols. Tags: ai, llm, claudecode, devtools. Source: `drafts/agents-dont-remember.md`. Published by PUT-promoting the existing draft (id 3977857) to avoid a duplicate — publish_devto.py always POSTs new.
- **URL**: https://dev.to/enjoy_kumawat/my-ai-agent-writes-great-code-and-forgets-all-of-it-by-tomorrow-32ec

### 2026-06-23 - OSS Contribution to google-gemini/gemini-cli — issue #28090
- **Status**: Completed (PR opened 2026-07-14, awaiting review) — unblocked via `gh auth refresh -s workflow`, rebased on upstream/main (clean), 93/93 shell tests green post-rebase, PR: https://github.com/google-gemini/gemini-cli/pull/28401
- **Description**: Triaged recent gemini-cli + google/adk-python issues for high merge-chance (no existing PR, clear scope). Picked #28090 (p1: shell tool forwards unbounded command output to the model). Root cause: `llmContent` built from full `result.output`; only mediation is opt-in `summarizeToolOutput`. Fix: added `truncateLlmOutput` (codepoint-safe head+tail byte bound, `MAX_LLM_OUTPUT_BYTES = 32 KiB`) in `packages/core/src/tools/shell.ts`, applied to normal-completion + aborted paths. Added 4 unit tests. Verified: 93/93 shell tests, tsc, eslint all clean; reported 40319-byte case → 32768 bytes. Branch `fix/28090-large-shell-output` in `codes/gemini-cli`, based on current `upstream/main`.
- **URL**: https://github.com/google-gemini/gemini-cli/issues/28090
- **Notes**: Forks cloned to `codes/` (adk-python, gemini-cli). Other live candidates with no PR: adk-python #6171 (eval), #6167 (services). Skipped #27790, adk #6174 (already had PRs).

### 2026-06-23 - DEV.to article: MCP security + /devto-article command
- **Status**: Completed (published live)
- **Description**: Built `/devto-article` slash command + `publish_devto.py` (reusable stdlib publisher; handles dev.to 403-bot UA, H1 strip, frontmatter). Test-ran it to draft, then published "I Build MCP Servers. Here's the Security Hole Nobody Talks About." (MCP prompt-injection / least-privilege). Tags: mcp, ai, security, claudecode. Source: `drafts/mcp-backdoor.md`.
- **URL**: https://dev.to/enjoy_kumawat/i-build-mcp-servers-heres-the-security-hole-nobody-talks-about-41b6

### 2026-06-23 - DEV.to article: Context Rot
- **Status**: Completed (published live)
- **Description**: Researched trending dev.to AI/LLM/Claude Code pain points; picked context-window degradation (most-discussed thread, 62 comments). Wrote + published "Context Rot: Why Your AI Coding Agent Gets Dumber Mid-Session (and How I Stopped It)" in established "real reason / my fix" voice, grounded in our `context-mode` setup. Tags: ai, claudecode, llm, productivity. Source: `drafts/context-rot.md`.
- **URL**: https://dev.to/enjoy_kumawat/context-rot-why-your-ai-coding-agent-gets-dumber-mid-session-and-how-i-stopped-it-3e9o

### 2026-06-21 - Phase 1: GitHub Profile README
- **Status**: Completed
- **Description**: Created `template.md` with animated typing SVG, tech stack badges, open source contribution badges, GitHub stats widgets. Auto-push via `update_profile.py` (stdlib-only).
- **Result**: Live at `github.com/enjoykumawat`

### 2026-06-21 - Phase 2: Developer Presence MCP Server
- **Status**: Completed
- **Description**: Built `server.py` — 7-tool FastMCP server combining GitHub REST API + DEV.to API. Tools: get_github_profile, list_repos, get_repo_stats, list_articles, create_article, update_article, get_article_stats.
- **Next**: Publish as public repo + write DEV.to article to drive visibility

### 2026-06-21 - Phase 3: AI Commit Message Generator
- **Status**: Completed
- **Description**: Built `git_commit.py` (20-line standalone CLI) + `generate_commit_message` MCP tool. Uses `claude -p` subprocess (OAuth, no API key). Published dev.to article and pushed to `enjoykumawat/my-git-manager`.
- **URLs**: https://dev.to/enjoy_kumawat/i-fixed-the-ai-commit-messages-problem-in-50-lines-of-python-3a5a | https://github.com/enjoykumawat/my-git-manager

### 2026-06-22 - OSS Contribution to nodejs/undici — review response
- **Status**: Merged (confirmed 2026-07-14)
- **Description**: Addressed `metcoder95` feedback: removed full `## Pre-built interceptors` section from `Dispatcher.md` (-285 lines), replaced with 3-line pointer to `Interceptors.md`, added link from `dispatcher.compose()` section, removed 9 now-unused link references. Pushed as commit `fbac28a1` on branch `docs/add-interceptors-api-page`.
- **URL**: https://github.com/nodejs/undici/pull/5446

### 2026-06-21 - Option C: OSS Contribution to nodejs/undici
- **Status**: Completed (PR opened)
- **Description**: Added missing API docs for all 8 built-in interceptors (`dump`, `retry`, `redirect`, `decompress`, `responseError`, `dns`, `cache`, `deduplicate`). Created `docs/docs/api/Interceptors.md` + updated sidebar in `site.json`.
- **URL**: https://github.com/nodejs/undici/pull/5446

### 2026-06-21 - Deleted oh-my-codex repo
- **Status**: Completed (manual)
- **Description**: Deleted `enjoykumawat/oh-my-codex` via GitHub Settings UI (API token lacked delete_repo scope)
